/*
 * Contenido del tema "Comunicación asíncrona (UART)" (Tema 13).
 * GENERADO por herramientas/spec_a_tema.py desde datos/_specs/comunicacion-asincrona.json — no editar a mano; edita el spec y regenera.
 */
window.MPI = window.MPI || {};
MPI.contenidoTemas = MPI.contenidoTemas || {};
MPI.ejercicios = MPI.ejercicios || {};
MPI.registros = MPI.registros || {};

MPI.ejercicios["comunicacion-asincrona-bittime"] = {"titulo": "Bit-time y duración de la trama", "enunciado": "<p>Configuramos la UART de la Raspberry Pi en formato <strong>8N1</strong> a una velocidad de <strong>9600 baudios</strong>. Queremos caracterizar la temporización de la comunicación.</p>", "contexto": "<ul><li>1 baudio = 1 bit/s (la señal UART es binaria).</li><li>En 8N1 una trama lleva 1 bit de start + 8 de datos + 1 de stop = 10 bits.</li><li>Tiempo de bit: <code>T<sub>bit</sub> = 1 / baudios</code>; duración de trama: <code>T<sub>trama</sub> = N<sub>bits</sub> · T<sub>bit</sub></code>.</li></ul>", "preguntas": [{"texto": "¿Cuánto dura cada bit (bit-time) a 9600 baudios?", "opciones": ["≈ 104 µs", "≈ 9,6 µs", "≈ 1,04 ms", "≈ 9600 µs"], "correcta": 0, "explicacion": "<p>Como 1 baudio = 1 bit/s, el bit-time es <code>T<sub>bit</sub> = 1 / 9600 s ≈ 1,042 × 10⁻⁴ s = 104 µs</code>.</p>"}, {"texto": "¿Cuánto tarda en transmitirse una trama 8N1 completa?", "opciones": ["≈ 0,104 ms (solo los 8 bits de datos)", "≈ 1,04 ms (los 10 bits de la trama)", "≈ 10 ms", "≈ 104 µs"], "correcta": 1, "explicacion": "<p>La trama 8N1 son 10 bits (start + 8 datos + stop). <code>T<sub>trama</sub> = 10 · T<sub>bit</sub> = 10 / 9600 s ≈ 1,04 ms</code>. Ojo: hay que contar el start y el stop, no solo los datos.</p>"}, {"texto": "¿Cuál es la velocidad útil aproximada en bytes/s con este formato?", "opciones": ["9600 bytes/s", "≈ 960 bytes/s", "≈ 1200 bytes/s", "≈ 115200 bytes/s"], "correcta": 1, "explicacion": "<p>En 8N1 se transmiten 10 bits por byte, así que la velocidad útil es <code>9600 / 10 = 960 bytes/s</code>. Solo 8 de cada 10 bits son datos útiles (eficiencia del 80 %).</p>"}]};
MPI.ejercicios["comunicacion-asincrona-examen"] = {"titulo": "Test de comunicaciones (Examen Ordinario 2026): UART", "enunciado": "<p>El <strong>Examen Ordinario 2026</strong> incluyó preguntas test de comunicaciones (UART, I²C y SPI). Practica con estas del mismo tipo sobre la <strong>UART</strong>:</p>", "contexto": "<ul><li>La UART es <strong>asíncrona</strong>: no se transmite ninguna señal de reloj por el bus.</li><li>Formato <strong>8N1</strong>: 1 bit de start + 8 bits de datos + 1 bit de stop.</li><li>1 baudio = 1 bit/s; <code>T<sub>bit</sub> = 1 / baudios</code>.</li></ul>", "preguntas": [{"texto": "¿Cuántos bits ocupa en total una trama en formato 8N1?", "opciones": ["8 bits (solo los datos)", "9 bits (datos + stop)", "10 bits (start + 8 datos + stop)", "11 bits (start + 8 datos + paridad + stop)"], "correcta": 2, "explicacion": "<p>En 8N1 hay <strong>1 bit de start + 8 bits de datos + 1 bit de stop = 10 bits</strong>. La «N» indica que no hay bit de paridad. Por eso la velocidad útil en bytes/s es baudios/10.</p>"}, {"texto": "¿Por qué emisor y receptor deben acordar de antemano la velocidad (baudios) en una transmisión UART?", "opciones": ["Porque la UART es asíncrona y no comparte una señal de reloj por el bus: cada uno usa su propio reloj y necesitan saber la duración de cada bit.", "Porque el bit de paridad solo se puede calcular si se conoce la velocidad.", "Porque la línea de reloj (SCL) se transmite junto con los datos y hay que sincronizarla.", "Porque la velocidad determina el número de bits de datos de la trama."], "correcta": 0, "explicacion": "<p>La UART es <strong>asíncrona</strong>: no envía ninguna señal de reloj por el bus. El receptor detecta el flanco del bit de start y, conociendo la velocidad acordada (el bit-time = 1/baudios), cuenta el tiempo para muestrear cada bit. Si las velocidades no coincidieran, los bits se leerían mal. La línea SCL es del I²C, no de la UART.</p>"}]};
MPI.ejercicios["comunicacion-asincrona-formato"] = {"titulo": "Identificar el formato de la trama", "enunciado": "<p>Observamos en un analizador lógico una trama serie asíncrona con la siguiente estructura: línea en reposo a 1, un bit a 0, ocho bits de datos, <strong>un bit de paridad</strong> calculado para que el número total de unos sea <strong>par</strong>, y <strong>dos bits a 1</strong> antes de volver al reposo.</p>", "contexto": "<ul><li>Nomenclatura: <em>bits de datos</em> + <em>paridad</em> (N/E/O) + <em>bits de stop</em>.</li><li>N = None (sin paridad), E = Even (par), O = Odd (impar).</li></ul>", "preguntas": [{"texto": "¿Qué formato corresponde a esta trama?", "opciones": ["8N1", "8E2", "8O2", "8E1"], "correcta": 1, "explicacion": "<p>Son 8 bits de datos, paridad <strong>par</strong> (E, Even) y <strong>2</strong> bits de stop: el formato es <strong>8E2</strong>. El bit a 0 inicial es el start (no entra en la nomenclatura) y los dos bits a 1 finales son los stop.</p>"}, {"texto": "¿Cuántos bits ocupa en total la trama (sin contar el reposo)?", "opciones": ["10 bits", "11 bits", "12 bits", "8 bits"], "correcta": 2, "explicacion": "<p>1 start + 8 datos + 1 paridad + 2 stop = <strong>12 bits</strong>. Solo 8 de los 12 son datos útiles, así que esta trama es menos eficiente que un 8N1 (10 bits).</p>"}]};

MPI.contenidoTemas["comunicacion-asincrona"] = {
  titulo: "Comunicación asíncrona (UART)",
  html: "<header class=\"tema-cab\">\n<span class=\"tema-num\">Tema 13</span>\n<h1>Comunicación asíncrona (UART)</h1>\n<p class=\"tema-lead\">Cuando hay que comunicar dos sistemas digitales a más de unos pocos centímetros, el bus paralelo deja de ser práctico y se opta por una <strong>línea serie</strong>. La forma más sencilla es la <strong>transmisión serie asíncrona</strong>, donde no se envía reloj: emisor y receptor se ponen de acuerdo en la velocidad y se resincronizan en cada byte con un <strong>bit de start</strong>. Este tema estudia la trama serie, sus estándares y cómo usar la <strong>UART de la Raspberry Pi 4</strong> desde Python con pyserial, incluido el puerto serie por Bluetooth.</p>\n</header>\n<section>\n<h2>13.1 Comunicación paralelo frente a serie</h2>\n<p>Para intercambiar datos entre dos sistemas digitales hay dos posibilidades:</p>\n<ul><li><strong>Bus paralelo:</strong> los <em>N</em> bits se mandan a la vez, por lo que hacen falta <em>N</em> cables. Es <strong>más rápido</strong>, pero solo funciona en <strong>distancias cortas</strong> (pocos cm) por el coste del cableado y las capacidades parásitas e interferencias entre cables. Se usa, p. ej., entre la CPU y la RAM de un ordenador (DDR5: bus de 64 bits, hasta 69 GB/s).</li><li><strong>Línea serie:</strong> los bits se mandan uno tras otro por el mismo cable. Es <strong>más lenta</strong>, pero permite <strong>distancias largas</strong> con menos cableado. Los interfaces serie más usados hoy son la <strong>UART</strong> (asíncrono), el bus <strong>I²C</strong>, el <strong>SPI</strong> y el <strong>USB</strong>.</li></ul>\n<p>La Raspberry Pi dispone de los cuatro interfaces serie más habituales (UART, I²C, SPI y USB). En este tema estudiamos solo la <strong>UART</strong>, que aunque es la menos frecuente para conectar periféricos, resulta muy útil para conectar un ordenador o un teléfono móvil a la Raspberry Pi.</p>\n</section>\n<section>\n<h2>13.2 La trama serie asíncrona</h2>\n<p>La característica clave de este método es que <strong>no se transmite ninguna señal de reloj</strong> por el bus (de ahí lo de <em>asíncrono</em>). ¿Cómo se ponen de acuerdo entonces emisor y receptor sobre cuándo enviar y leer cada bit?</p><ol><li>Primero <strong>acuerdan la velocidad de transmisión</strong>. Si transmiten a 1 kb/s, cada milisegundo se envía un bit.</li><li>La línea, en reposo, está a <strong>nivel alto (1)</strong>.</li><li>Cuando el transmisor quiere enviar un dato, manda primero un <strong>bit de start</strong> (un 0). El receptor detecta su <strong>flanco de bajada</strong> y, a partir de ahí, cuenta el tiempo: dentro de un bit-time llegará el primer bit de datos, etc.</li><li>Tras los datos se envía el <strong>bit de stop</strong> (un 1), que deja la línea en reposo el tiempo necesario para poder enviar el siguiente bit de start.</li></ol><p>El emisor introduce el dato en el flanco de subida de su reloj y el receptor lo lee en el de bajada del suyo. Los bits de datos se envían <strong>empezando por el menos significativo (LSB)</strong>.</p>\n<p>El inconveniente de las transmisiones asíncronas es que los relojes de emisor y receptor nunca son idénticos. Si se enviasen muchos bits seguidos, los últimos se muestrearían con un desfase creciente y se leerían mal. Por eso las transmisiones asíncronas se hacen <strong>byte a byte</strong>, intercalando un bit de start y uno de stop entre cada byte para <strong>resincronizar los relojes</strong> en cada trama.</p>\n<p>El bit de stop es normalmente de 1 bit, pero en algunos sistemas lentos puede ser de 1,5 o 2 bits. Mueve el formato, la paridad y los baudios en el visualizador para ver la trama, cuántos bits ocupa y cuánto dura cada bit y la trama completa:</p>\n<div class=\"mpi-mount\" data-componente=\"trama-uart\" data-config='{}'></div>\n</section>\n<section>\n<h2>13.3 La paridad</h2>\n<p>En la mayoría de sistemas actuales se envían <strong>1 bit de start, 8 bits de datos sin paridad y 1 bit de stop</strong>. No obstante, se puede añadir un <strong>bit de paridad</strong> (después de los datos) para detectar errores de transmisión, útiles en ambientes ruidosos.</p>\n<ul><li><strong>Sin paridad</strong> (<em>None</em>).</li><li><strong>Paridad par</strong> (<em>Even</em>): el bit de paridad se elige para que el número total de unos (incluido el de paridad) sea <strong>par</strong>.</li><li><strong>Paridad impar</strong> (<em>Odd</em>): el número total de unos sea <strong>impar</strong>.</li></ul>\n<p>El emisor calcula la paridad; el receptor la vuelve a verificar y, si no cuadra, señala un error. Qué hacer entonces (avisar al usuario, volver a pedir el dato…) es responsabilidad del <strong>software</strong>. Ejemplo: para el dato <code>0011&nbsp;0100</code> con paridad <strong>par</strong>, como el dato tiene 3 unos (impar), hay que poner el bit de paridad a <strong>1</strong> para que el total de unos sea par. Recuerda que en la trama se envía primero el bit menos significativo.</p>\n<p><strong>Nomenclatura.</strong> El formato de una trama se abrevia con tres caracteres: <em>bits de datos</em>, <em>paridad</em> y <em>bits de stop</em>. La paridad se indica con <strong>N</strong> (None), <strong>E</strong> (Even) u <strong>O</strong> (Odd):</p><table class=\"tabla-datos\"><thead><tr><th>Formato</th><th>Datos</th><th>Paridad</th><th>Stop</th><th>Bits/byte</th></tr></thead><tbody><tr><td><strong>8N1</strong></td><td>8</td><td>ninguna</td><td>1</td><td>10</td></tr><tr><td><strong>8E1</strong></td><td>8</td><td>par</td><td>1</td><td>11</td></tr><tr><td><strong>8O2</strong></td><td>8</td><td>impar</td><td>2</td><td>12</td></tr></tbody></table><p>(El número de bits de datos se especifica porque en los primeros códigos de caracteres se usaban 7 bits; hoy siempre son 8, pero la nomenclatura se mantiene.)</p>\n</section>\n<section>\n<h2>13.4 Velocidad: baudios y bit-time</h2>\n<p>La velocidad de transmisión se mide en <strong>baudios</strong>, que son <strong>transiciones (símbolos) por segundo</strong>. Como la señal UART es binaria (cada símbolo es un bit), <strong>1 baudio = 1 bit/s</strong>. En el cómputo en baudios se cuentan <em>todos</em> los bits, incluidos el de start y el de stop.</p>\n<p>El <strong>tiempo de bit</strong> (bit-time) es el inverso de la velocidad en baudios:</p><math class=\"formula\" xmlns=\"http://www.w3.org/1998/Math/MathML\" display=\"block\"><mrow><msub><mi>T</mi><mtext>bit</mtext></msub><mo>=</mo><mfrac><mn>1</mn><mtext>baudios</mtext></mfrac></mrow></math>\n<p>La <strong>duración de una trama</strong> es el número total de bits (start + datos + paridad + stop) por el tiempo de bit. Por ejemplo, una trama 8N1 (10 bits) a 9600 baudios:</p><math class=\"formula\" xmlns=\"http://www.w3.org/1998/Math/MathML\" display=\"block\"><mrow><msub><mi>T</mi><mtext>trama</mtext></msub><mo>=</mo><msub><mi>N</mi><mtext>bits</mtext></msub><mo>&#183;</mo><msub><mi>T</mi><mtext>bit</mtext></msub><mo>=</mo><mfrac><msub><mi>N</mi><mtext>bits</mtext></msub><mtext>baudios</mtext></mfrac><mo>=</mo><mfrac><mn>10</mn><mn>9600</mn></mfrac><mtext>&#160;s</mtext><mo>&#8776;</mo><mn>1</mn><mo>,</mo><mn>04</mn><mtext>&#160;ms</mtext></mrow></math>\n<p>Las velocidades estándar (en baudios), heredadas del RS-232, son: 75, 110, 134,5, 150, 300, 600, 1200, 1800, 2400, 4800, 7200, 9600, 14400, 19200, 38400, 56000, 57600 y <strong>115200</strong>. Para obtener la velocidad útil en <strong>bytes/s</strong> con el formato 8N1 (10 bits por byte) basta con dividir los baudios entre 10. Una comunicación es <strong>full-duplex</strong> si puede transmitir y recibir a la vez (líneas Tx y Rx independientes) y <strong>half-duplex</strong> si comparte un único canal para ambos sentidos.</p>\n</section>\n<section>\n<h2>13.5 El estándar RS-232 y los niveles de tensión</h2>\n<p>Los estándares serie asíncronos comparten la forma de enviar la información (start, datos, stop) y se diferencian en la <strong>capa física</strong> (conectores, cables, niveles) y el protocolo. El más clásico es el <strong>RS-232</strong> (1960), adoptado por los PC en los 80 para módems, impresoras, ratones… Hoy se sigue usando para comunicar PCs con sistemas empotrados.</p>\n<ul><li><strong>Niveles de tensión (lógica negativa):</strong> el nivel alto es una tensión entre &minus;3 V y &minus;15 V, y el nivel bajo entre +3 V y +15 V.</li><li><strong>Longitud máxima</strong> de cable: unos 15 m.</li><li><strong>Conector:</strong> originalmente DB25; los PC popularizaron el <strong>DB9</strong>.</li><li>Para conectar un PC con un sistema empotrado o un periférico basta con la <strong>tierra común</strong>, <strong>Rx</strong> (se conecta al Tx del otro equipo) y <strong>Tx</strong> (se conecta al Rx del otro).</li></ul>\n<p>Para controlar el <strong>flujo de datos</strong> (que el emisor pare si el receptor está ocupado) hay dos vías: por <strong>hardware</strong>, con las señales RTS/RTR (listo para recibir) y CTS (confirma que puede enviar), a costa de dos cables más; o por <strong>software</strong>, con el protocolo <strong>XON/XOFF</strong>, que usa dos caracteres no imprimibles para pausar y reanudar la transmisión (no es automático, lo gestiona el software).</p>\n<div class=\"nota\"><strong>¡Cuidado con las tensiones!</strong> La UART de la Raspberry Pi trabaja a <strong>3,3 V (TTL)</strong>. Si la conectamos directamente al puerto RS-232 de un PC (±10 V) o a un dispositivo de 5 V, la Raspberry Pi <em>morirá en acto de servicio</em>. Para conectarla a un PC por puerto serie usa un <strong>adaptador USB-serie a 3,3 V</strong> (p. ej. los basados en el chip FT232RL de FTDI).</div>\n</section>\n<section>\n<h2>13.6 La UART de la Raspberry Pi 4</h2>\n<p>El dispositivo hardware que genera y recibe las tramas de bits se llama <strong>UART</strong> (<em>Universal Asynchronous Receiver-Transmitter</em>). La Raspberry Pi dispone de dos tipos:</p>\n<ul><li><strong>PL011:</strong> UART completa (compatible con el estándar industrial 16550). Soporta <strong>bit de paridad</strong>, <strong>control de flujo por hardware</strong> y las líneas de control de módem (DSR/RI/DCD).</li><li><strong>miniUART:</strong> más básica. <strong>No</strong> soporta bit de paridad ni líneas de control de módem; para la mayoría de aplicaciones es suficiente.</li></ul>\n<p>En la Raspberry Pi 4 hay <strong>1 miniUART y 5 PL011</strong> (por defecto solo una activa). La UART <strong>primaria</strong> (la miniUART) se conecta a los pines <strong>GPIO14 (Tx)</strong> y <strong>GPIO15 (Rx)</strong>, y se accede en Raspbian por el dispositivo <code>/dev/serial0</code>. La UART <strong>secundaria</strong> (una PL011) está conectada al chip de <strong>Bluetooth</strong>.</p>\n<p><strong>Configuración de <code>/dev/serial0</code>.</strong> La miniUART está inhabilitada por defecto, y la UART primaria viene configurada como consola de Linux. Para usarla desde Python hay que <strong>habilitar el puerto serie</strong> y <strong>deshabilitar la consola</strong>. Lo más sencillo es ejecutar <code>sudo raspi-config</code>:</p>\n<ol><li>Ejecuta <code>sudo raspi-config</code>.</li><li>Opción <strong>3 (Interface Options)</strong>.</li><li>Opción <strong>I3 (Serial Port)</strong>.</li><li>«¿Tener accesible un login shell por el puerto serie?» &rarr; <strong>No</strong>.</li><li>«¿Habilitar el puerto serie?» &rarr; <strong>Sí</strong>.</li><li>Aparece <em>«The serial login shell is disabled. The serial interface is enabled»</em>. Sal y <strong>reinicia</strong> cuando lo pida.</li></ol>\n</section>\n<section>\n<h2>13.7 Uso de la UART desde Python (pyserial)</h2>\n<p>Para usar la UART desde Python tenemos la librería <strong>pyserial</strong>, instalada de serie en Raspbian. Consta de una sola clase, <code>Serial</code>, que se instancia indicando el <strong>puerto</strong> y la <strong>velocidad</strong> en baudios. El resto de parámetros (número de bits, paridad, bits de stop…) son opcionales; por defecto el puerto se configura como <strong>8N1</strong>.</p>\n<p>Inicialización del puerto serie primario a 115200 baudios (8N1 por defecto). <strong>Ojo:</strong> en <code>/dev/serial0</code> el penúltimo carácter es una ele (de <em>serial</em>), no un uno.</p><pre><code class=\"lang-python\">from serial import Serial\n\n# 8N1 (valor por defecto) a 115200 baudios\nserie = Serial(\"/dev/serial0\", 115200)\n\n# Equivalente indicando paridad y stop explícitamente:\n# serie = Serial(\"/dev/serial0\", 115200,\n#                parity=serial.PARITY_NONE,\n#                stopbits=serial.STOPBITS_ONE)</code></pre>\n<p><strong>Enviar datos con <code>write()</code>.</strong> En Python las cadenas son Unicode, pero el puerto serie envía bytes (ASCII). Hay que crear directamente una cadena de bytes (prefijo <code>b</code>) o convertir una cadena con <code>encode()</code>:</p><pre><code class=\"lang-python\">serie.write(b\"Hola mundo\")          # cadena de bytes directa\n\ncadena = \"Hola mundo\"\ncad_bytes = cadena.encode()         # str (Unicode) -&gt; bytes\nserie.write(cad_bytes)</code></pre>\n<p><strong>Leer datos con <code>read(size=1)</code>.</strong> Lee <code>size</code> caracteres del puerto serie y <strong>se bloquea</strong> hasta que lleguen. Se le puede añadir un <em>timeout</em> al constructor para que retorne aunque no llegue nada; en ese caso devuelve los bytes realmente leídos. Cada byte leído se pasa a <code>str</code> con <code>decode()</code>.</p>\n<p><strong>Lectura de una cadena</strong> enviada desde un terminal serie que termina en <code>\\r</code> (retorno de carro al pulsar enter), haciendo <strong>eco</strong> de cada carácter recibido:</p><pre><code class=\"lang-python\">cadena = \"\"            # cadena a leer del puerto serie\ncar = serie.read()     # lee un carácter (1 byte, bloqueante)\nwhile car != b'\\r':\n    cadena += car.decode()  # añado el carácter (byte -&gt; str)\n    serie.write(car)        # eco al terminal\n    car = serie.read()</code></pre>\n</section>\n<section>\n<h2>13.8 Ejemplo: encender los LED de la iMAT HAT desde el PC</h2>\n<p>Desde un terminal serie del PC (p. ej. <strong>CoolTerm</strong>) controlamos los LED de la iMAT HAT. Para encender un LED se envía la cadena <code>N on</code>, siendo <code>N</code> el número del LED (0 para GPIO20, 1 para GPIO21…); para apagarlo, <code>N off</code>. El bucle lee el comando carácter a carácter hasta el <code>\\r</code>, lo parte por el espacio y actúa sobre el LED:</p>\n<pre><code class=\"lang-python\">from gpiozero import LEDBoard\nfrom serial import Serial\n\nleds = LEDBoard(20, 21, 22, 23, 24, 25, 26, 27)\nserie = Serial(\"/dev/serial0\", baudrate=115200)\n\nwhile True:\n    serie.write(b'\\nTeclee el LED y su estado. Por ejemplo \"0 on\" &gt; ')\n    comando = \"\"            # comando a leer del puerto serie\n    car = serie.read()     # lee un carácter\n    while car != b'\\r':\n        comando += car.decode()  # añado el carácter al comando\n        serie.write(car)         # eco al terminal\n        car = serie.read()\n\n    comando_lst = comando.split(\" \")   # separar el nº de LED y la orden\n    num_led = int(comando_lst[0])      # el nº de LED es texto: a entero\n    orden = comando_lst[1]\n    if orden == 'on':\n        leds[num_led].on()\n    else:\n        leds[num_led].off()</code></pre>\n<p>Se crea un <code>LEDBoard</code> para acceder a los ocho LED y un objeto <code>Serial</code>. En el bucle se imprime la instrucción, se lee el comando hasta el enter, se divide con <code>split(\" \")</code> en número de LED y orden, se convierte el número a entero con <code>int()</code> y, según la orden, se enciende o apaga el LED correspondiente.</p>\n</section>\n<section>\n<h2>13.9 Puerto serie por Bluetooth (rfcomm0)</h2>\n<p>La UART secundaria está conectada al chip de <strong>Bluetooth</strong>, así que podemos crear un puerto serie por Bluetooth y conectarnos desde un PC o un móvil (p. ej. con la app <em>Serial Bluetooth Terminal</em>). Esto es muy útil, por ejemplo, para controlar un robot móvil de forma remota.</p><p>Una vez <strong>vinculado</strong> el móvil con <code>bluetoothctl</code> (la Raspberry aparece como <code>imat-se-xx</code>) y arrancado el puerto serie, se puede reutilizar el programa anterior de los LED cambiando solo el dispositivo y la velocidad:</p>\n<p>Arranque del puerto serie por Bluetooth en la Raspberry Pi (se queda esperando la conexión; usa otro terminal para interactuar):</p><pre><code class=\"lang-python\">sudo sdptool add --channel=1 SP\nsudo rfcomm watch hci0 1 /dev/rfcomm0</code></pre>\n<p>En el programa de Python basta con cambiar la línea de creación del objeto serie; el terminal Bluetooth usa <strong>9600 baudios</strong> por defecto:</p><pre><code class=\"lang-python\">serie = Serial(\"/dev/rfcomm0\", baudrate=9600)</code></pre>\n<p>Tras esto se pueden encender y apagar los LED de la iMAT HAT desde el móvil enviando comandos como <code>1 on</code> o <code>2 off</code>.</p>\n</section>\n<section>\n<h2>Ejercicios</h2>\n<div class=\"mpi-mount\" data-componente=\"ejercicio-driver\" data-config='{\"ref\": \"comunicacion-asincrona-bittime\"}'></div>\n<div class=\"mpi-mount\" data-componente=\"ejercicio-driver\" data-config='{\"ref\": \"comunicacion-asincrona-formato\"}'></div>\n<div class=\"mpi-mount\" data-componente=\"ejercicio-driver\" data-config='{\"ref\": \"comunicacion-asincrona-examen\"}'></div>\n</section>\n<section class=\"tema-cierre\"><h2>Resumen</h2><ul>\n<li>El bus <strong>paralelo</strong> es rápido pero corto; la <strong>línea serie</strong> es más lenta pero permite distancias largas con menos cableado. La UART es serie y <strong>asíncrona</strong> (no transmite reloj).</li>\n<li>La <strong>trama serie asíncrona</strong>: línea en reposo a 1, <strong>bit de start</strong> (0, su flanco sincroniza), bits de <strong>datos</strong> (LSB primero), <strong>paridad</strong> opcional (N/E/O) y <strong>bit(s) de stop</strong> (1). Se transmite byte a byte para resincronizar los relojes.</li>\n<li>Nomenclatura <strong>8N1 / 8E1 / 8O2</strong> = datos + paridad + stop. <strong>1 baudio = 1 bit/s</strong>; <code>T<sub>bit</sub> = 1/baudios</code> y <code>T<sub>trama</sub> = N<sub>bits</sub>/baudios</code>. En 8N1 hay 10 bits/byte, así que bytes/s = baudios/10.</li>\n<li>El <strong>RS-232</strong> usa lógica negativa (±3–15 V); la UART de la Pi trabaja a <strong>3,3 V (TTL)</strong>, por lo que para conectarla a un PC hace falta un <strong>adaptador USB-serie a 3,3 V</strong>.</li>\n<li>La Pi 4 tiene <strong>PL011</strong> (con paridad y control de flujo) y <strong>miniUART</strong>. La primaria está en <code>/dev/serial0</code> (GPIO14 Tx, GPIO15 Rx); se habilita con <code>raspi-config</code> (login shell No, puerto serie Sí).</li>\n<li>Con <strong>pyserial</strong>: <code>Serial(\"/dev/serial0\", 115200)</code> (8N1 por defecto), <code>write(b\"...\")</code> o <code>cadena.encode()</code>, y <code>read()</code> (1 byte, bloqueante) + <code>decode()</code>. La UART secundaria es Bluetooth: <code>Serial(\"/dev/rfcomm0\", baudrate=9600)</code>.</li>\n</ul></section>"
};
