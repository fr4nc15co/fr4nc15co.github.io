/*
 * Contenido del tema "Ejemplo integrador: el termostato" (Tema 12).
 * GENERADO por herramientas/spec_a_tema.py desde datos/_specs/ejemplo-integrador-termostato.json — no editar a mano; edita el spec y regenera.
 */
window.MPI = window.MPI || {};
MPI.contenidoTemas = MPI.contenidoTemas || {};
MPI.ejercicios = MPI.ejercicios || {};
MPI.registros = MPI.registros || {};

if (!MPI.registros["OC4CON"]) MPI.registros["OC4CON"] = {"titulo": "OC4CON: Output Compare 4 Control Register", "nota": "Reconstruido del datasheet DS60001168 (Register 16-1). Los bits 31-16 no están implementados; se muestran los 16 bits bajos. Las cinco salidas OC1 a OC5 tienen un OCxCON idéntico.", "ancho": 16, "campos": [{"bit": 15, "nombre": "ON", "desc": "Output Compare Peripheral On: enciende o apaga el módulo. Apagarlo es la forma correcta de no consumir cuando el ventilador no se usa (mejor que OCM = 000).", "valores": {"0": "Módulo apagado", "1": "Módulo encendido"}}, {"bit": 13, "nombre": "SIDL", "desc": "Stop in Idle Mode: comportamiento cuando la CPU entra en modo Idle.", "valores": {"0": "Sigue funcionando en Idle", "1": "Se detiene en Idle"}}, {"bit": 5, "nombre": "OC32", "desc": "32-bit Compare Mode: la comparación se hace contra un temporizador de 16 o de 32 bits.", "valores": {"0": "Temporizador de 16 bits (Timer2 o Timer3)", "1": "Temporizador de 32 bits (Timer2+Timer3 en cascada)"}}, {"bit": 4, "nombre": "OCFLT", "desc": "PWM Fault Condition Status (solo lectura): indica si se ha producido una falta PWM. Solo se usa con OCM = 111.", "valores": {"0": "Sin falta PWM", "1": "Falta PWM detectada (salida desconectada)"}}, {"bit": 3, "nombre": "OCTSEL", "desc": "Output Compare Timer Select: temporizador usado como base de tiempos en modo de 16 bits. En el termostato se usa el Timer 2 (0).", "valores": {"0": "Timer 2", "1": "Timer 3"}}, {"bits": [2, 0], "nombre": "OCM", "desc": "Output Compare Mode: modo de funcionamiento del módulo. Para el ventilador se usa 110 (PWM sin detección de faltas).", "valores": {"0": "Salida inactiva (el módulo sigue consumiendo)", "3": "La salida conmuta al llegar a OCxR", "5": "Pulsos continuos (OCxR sube, OCxRS baja)", "6": "PWM sin detección de faltas", "7": "PWM con detección de faltas (entrada OCFA/OCFB)"}}], "nombre": "OC4CON"};
if (!MPI.registros["T2CON"]) MPI.registros["T2CON"] = {"titulo": "T2CON: Timer 2 Control Register (tipo B)", "nota": "Reconstruido del datasheet DS60001168 (Register 13-1). Los temporizadores 2 a 5 (tipo B) comparten este formato; el prescaler tiene 8 valores (TCKPS de 3 bits) frente a los 4 del Timer 1.", "ancho": 16, "campos": [{"bit": 15, "nombre": "ON", "desc": "Timer On: enciende o apaga el temporizador. Se apaga durante la configuración para evitar comparaciones falsas al escribir PR2.", "valores": {"0": "Temporizador parado", "1": "Temporizador en marcha"}}, {"bit": 13, "nombre": "SIDL", "desc": "Stop in Idle Mode: comportamiento al entrar la CPU en modo Idle.", "valores": {"0": "Sigue contando en Idle", "1": "Se detiene en Idle"}}, {"bit": 7, "nombre": "TGATE", "desc": "Gated Time Accumulation Enable: con TCS = 0, el reloj solo llega al contador mientras la entrada TxCK está a 1 (sirve para medir anchos de pulso).", "valores": {"0": "Modo gated deshabilitado", "1": "Modo gated habilitado"}}, {"bits": [6, 4], "nombre": "TCKPS", "desc": "Timer Clock Prescale: divisor del reloj. Vale 2^TCKPS salvo el 7, que divide por 256. Para los 50 µs del PWM basta 1:1.", "valores": {"0": "1:1", "1": "1:2", "2": "1:4", "3": "1:8", "4": "1:16", "5": "1:32", "6": "1:64", "7": "1:256"}}, {"bit": 3, "nombre": "T32", "desc": "32-Bit Timer Mode: une el Timer 2 (maestro) con el Timer 3 (esclavo) para formar un temporizador de 32 bits.", "valores": {"0": "Dos temporizadores de 16 bits independientes", "1": "Timer 2 + Timer 3 como temporizador de 32 bits"}}, {"bit": 1, "nombre": "TCS", "desc": "Timer Clock Source: origen del reloj del contador.", "valores": {"0": "Reloj interno PBCLK (lo habitual)", "1": "Reloj externo por el pin TxCK"}}], "nombre": "T2CON"};
if (!MPI.registros["AD1CON1"]) MPI.registros["AD1CON1"] = {"titulo": "AD1CON1: ADC Control Register 1", "nota": "Reconstruido del datasheet DS60001168 (Register 22-1). Los bits 31-16 no están implementados. En el termostato queda a 0x8044: ON = 1, SSRC = 010 (Timer 3) y ASAM = 1.", "ancho": 16, "campos": [{"bit": 15, "nombre": "ON", "desc": "ADC Operating Mode: enciende el conversor. Se pone a 1 al final de la configuración del ADC (pero antes de arrancar el Timer 3 que lo dispara).", "valores": {"0": "ADC apagado", "1": "ADC en funcionamiento"}}, {"bit": 13, "nombre": "SIDL", "desc": "Stop in Idle Mode: comportamiento al entrar la CPU en modo Idle.", "valores": {"0": "Sigue funcionando en Idle", "1": "Se detiene en Idle"}}, {"bits": [10, 8], "nombre": "FORM", "desc": "Data Output Format: formato del resultado en ADC1BUFx. En la asignatura se usa siempre el entero de 16 bits sin signo (000).", "valores": {"0": "Entero 16 bits sin signo (0 a 1023)", "1": "Entero 16 bits con signo", "4": "Entero 32 bits sin signo", "7": "Fracción 32 bits con signo"}}, {"bits": [7, 5], "nombre": "SSRC", "desc": "Conversion Trigger Source Select: quién dispara el fin del muestreo y el comienzo de la conversión. El termostato usa 010 (Timer 3).", "valores": {"0": "Manual: poner SAMP a 0 inicia la conversión", "1": "Transición activa en INT0", "2": "Fin de cuenta del Timer 3", "7": "Autoconversión: contador interno (dura SAMC ciclos)"}}, {"bit": 4, "nombre": "CLRASAM", "desc": "Stop Conversion Sequence: si vale 1, tras la primera interrupción el hardware borra ASAM y deja de convertir (el buffer no se sobreescribe).", "valores": {"0": "Funcionamiento normal (se sigue convirtiendo)", "1": "Para tras la primera interrupción"}}, {"bit": 2, "nombre": "ASAM", "desc": "ADC Sample Auto-Start: el muestreo arranca solo al terminar cada conversión, sin tener que poner SAMP a 1 por software.", "valores": {"0": "El muestreo empieza al escribir SAMP = 1", "1": "Muestreo automático tras cada conversión"}}, {"bit": 1, "nombre": "SAMP", "desc": "ADC Sample Enable: estado del sample&hold. Con ASAM = 1 lo gestiona el hardware; en modo manual lo escribe el software.", "valores": {"0": "S&H reteniendo (hold)", "1": "S&H muestreando"}}, {"bit": 0, "nombre": "DONE", "desc": "Conversion Status: lo pone el hardware al terminar una conversión. Con SMPI > 0 no sirve para esperar la secuencia completa (para eso está AD1IF).", "valores": {"0": "Conversión en curso o no iniciada", "1": "Conversión terminada"}}], "nombre": "AD1CON1"};
if (!MPI.registros["AD1CHS"]) MPI.registros["AD1CHS"] = {"titulo": "AD1CHS: ADC Input Select Register", "nota": "Reconstruido del datasheet DS60001168 (Register 22-4). Selecciona las entradas del amplificador diferencial. 0x00050000 = CH0SA 0101 (AN5, el potenciómetro) con CH0NA = 0 (Vref-). La ISR del termostato lo reescribe para alternar con AN0.", "ancho": 32, "campos": [{"bit": 31, "nombre": "CH0NB", "desc": "Negative Input Select for Sample B: entrada negativa del mux B (solo se usa si se alternan los muxes con ALTS).", "valores": {"0": "Entrada negativa = Vref- (0 V)", "1": "Entrada negativa = AN1 (medida diferencial)"}}, {"bits": [27, 24], "nombre": "CH0SB", "desc": "Positive Input Select for Sample B: canal de la entrada positiva del mux B (0 = AN0 ... 12 = AN12). No se usa en el termostato.", "valores": {"0": "AN0", "5": "AN5", "12": "AN12"}}, {"bit": 23, "nombre": "CH0NA", "desc": "Negative Input Select for Sample A: entrada negativa del mux A. Para medidas unipolares (las normales) se deja a 0.", "valores": {"0": "Entrada negativa = Vref- (0 V)", "1": "Entrada negativa = AN1 (medida diferencial)"}}, {"bits": [19, 16], "nombre": "CH0SA", "desc": "Positive Input Select for Sample A: canal de la entrada positiva del mux A. El termostato alterna 5 (AN5, potenciómetro) y 0 (AN0, sensor) desde la ISR.", "valores": {"0": "AN0 (sensor de temperatura)", "5": "AN5 (potenciómetro de referencia)", "12": "AN12", "13": "Sensor de temperatura interno (CTMU)"}}], "nombre": "AD1CHS"};
if (!MPI.registros["AD1CON3"]) MPI.registros["AD1CON3"] = {"titulo": "AD1CON3: ADC Control Register 3", "nota": "Reconstruido del datasheet DS60001168 (Register 22-3). Controla el reloj del conversor y el tiempo de automuestreo. 0x0200 = SAMC 2 y ADCS 0 (TAD = 2·TPBCLK, a tope); con SSRC = 2 el campo SAMC queda escrito pero no interviene.", "ancho": 16, "campos": [{"bit": 15, "nombre": "ADRC", "desc": "ADC Conversion Clock Source: origen del reloj del conversor.", "valores": {"0": "Derivado del reloj de periféricos PBCLK", "1": "Oscilador RC interno FRC (sigue activo en bajo consumo)"}}, {"bits": [12, 8], "nombre": "SAMC", "desc": "Auto-Sample Time: ciclos TAD que dura el muestreo en modo autoconversión (solo se usa con SSRC = 111; con disparo por timer o INT0 el fin del muestreo lo marca el propio disparo). Entre 1 y 31; el 0 está prohibido.", "valores": {"1": "1 TAD", "2": "2 TAD (valor escrito en el termostato)", "31": "31 TAD (máximo)"}}, {"bits": [7, 0], "nombre": "ADCS", "desc": "ADC Conversion Clock Select: divisor del PBCLK. TAD = TPBCLK · 2 · (ADCS + 1). Con ADCS = 0 y PBCLK = 5 MHz, TAD = 400 ns (la máxima velocidad).", "valores": {"0": "TAD = 2·TPBCLK", "1": "TAD = 4·TPBCLK", "255": "TAD = 512·TPBCLK"}}], "nombre": "AD1CON3"};
MPI.ejercicios["ejemplo-integrador-termostato-1"] = {"titulo": "Driver del ventilador: PWM a 20 kHz con OC4", "enunciado": "<p>Siguiendo el esquema del termostato de clase (y muy parecido al módulo de motores del examen de <strong>mayo de 2023</strong>, donde se pedían dos PWM de 20&nbsp;kHz con OC4 y OC5), se pide implementar el driver del ventilador: la salida <strong>OC4</strong> se conecta al pin <strong>RC6</strong> y se usa el <strong>Timer&nbsp;2</strong> como base de tiempos para generar una señal PWM de <strong>20&nbsp;kHz</strong>. La función <code>void velVent(uint8_t intensidad)</code> debe fijar el factor de servicio a 0/3, 1/3, 2/3 o 3/3 según la velocidad pedida (0 a 3).</p>", "contexto": "<ul><li>PBCLK = 5&nbsp;MHz.</li><li>RC6 es el pin remapeable RPC6; en su grupo de la tabla OUTPUT PIN SELECTION, el valor 5 corresponde a OC4.</li><li><code>#define PERIODO 249</code>.</li><li>El driver se inicializa una sola vez antes del <code>while</code> y usa máscaras para no tocar bits ajenos.</li></ul>", "preguntas": [{"texto": "¿Qué valor hay que cargar en PR2 para que el PWM sea de 20 kHz con prescaler 1:1?", "opciones": ["249", "250", "4999", "124"], "correcta": 0, "explicacion": "<p>El periodo de la onda es <em>T</em> = 1/20&nbsp;kHz = 50&nbsp;µs. Aplicando PR2 = T·f<sub>PBCLK</sub>/prescaler − 1 = 50&nbsp;µs · 5&nbsp;MHz − 1 = 250 − 1 = <strong>249</strong>. El −1 es porque el temporizador cuenta desde 0; 250 generaría un periodo de 50,2&nbsp;µs.</p>"}, {"texto": "Para conectar OC4 al pin RC6, ¿qué hay que hacer antes de escribir RPC6R = 5?", "opciones": ["Nada: los registros RPxR se escriben directamente como cualquier SFR", "Ejecutar la secuencia de desbloqueo escribiendo 0xAA996655 y 0x556699AA en SYSKEY", "Configurar RC6 como entrada poniendo a 1 su bit de TRISC", "Poner a 1 el bit correspondiente de ANSELC para habilitar la función alternativa"], "correcta": 1, "explicacion": "<p>Los registros de remapeo están protegidos (IOLOCK) para que un error del programa no pueda «freír» el micro conectando una salida a otra salida. Hay que escribir en <code class=\"reg\">SYSKEY</code> los valores <code>0xAA996655</code> y <code>0x556699AA</code> seguidos, escribir <code>RPC6R = 5</code> y volver a bloquear escribiendo cualquier otro valor (por convenio, <code>0x1CA11CA1</code>). RC6 es una <em>salida</em> (TRISC6 = 0) y ANSEL no interviene en el remapeo.</p>"}, {"texto": "Con PERIODO = 249, ¿qué valor escribe velVent(2) en OC4RS para la velocidad media (2/3)?", "opciones": ["83", "125", "166", "249"], "correcta": 2, "explicacion": "<p><code>OC4RS = (intensidad · PERIODO) / 3 = (2 · 249) / 3 = 498 / 3 = <strong>166</strong></code>. El tiempo en alto es 166 cuentas de las 250 del periodo, es decir, un factor de servicio del 66,4&nbsp;% ≈ 2/3. Con intensidad 1 saldría 83 (1/3) y con 3, 249 (≈100&nbsp;%).</p>"}, {"texto": "¿Por qué iniOC() escribe OC4R = 0 antes de encender el módulo con OC4CON = 0x8006, si después velVent() solo toca OC4RS?", "opciones": ["Por simetría: OC4R y OC4RS deben tener siempre el mismo valor", "Porque en modo PWM OC4R no se puede escribir: fija el ancho del primer pulso y luego el hardware lo recarga desde OC4RS en cada periodo", "Porque OC4R es el registro de periodo y OC4RS el de duty", "Porque si OC4R no se inicializa, el módulo genera una falta PWM y activa OCFLT"], "correcta": 1, "explicacion": "<p>En modo PWM el módulo <strong>bloquea la escritura en OCxR</strong> mientras funciona: OCxR define el ancho del primer pulso y, al final de cada periodo, el hardware copia OCxRS en OCxR. Así el software actualiza el duty escribiendo en OCxRS sin riesgo de deformar la onda en curso (si se cambiara OCxR por debajo del valor actual del timer, la comparación se perdería y la salida quedaría un periodo entero a 1). Por eso el valor inicial hay que dejarlo escrito <em>antes</em> de poner ON = 1.</p>"}]};
MPI.ejercicios["ejemplo-integrador-termostato-2"] = {"titulo": "Driver del AD: dos canales, interrupciones y datos compartidos", "enunciado": "<p>El termostato mide dos señales analógicas con un solo <em>sample&amp;hold</em>: la referencia del potenciómetro en <strong>RB3/AN5</strong> (8 a 30&nbsp;°C) y el sensor de temperatura en <strong>RA0/AN0</strong> (2 a 50&nbsp;°C). El conversor trabaja en modo continuo disparado por el <strong>Timer&nbsp;3</strong> cada 1&nbsp;ms (SSRC&nbsp;=&nbsp;2, ASAM&nbsp;=&nbsp;1, SMPI&nbsp;=&nbsp;0) y la rutina de interrupción (vector 23) alterna el canal reescribiendo <code class=\"reg\">AD1CHS</code>. Los getters <code>getTemp()</code> y <code>getRef()</code> convierten la lectura a grados. Es el mismo esquema que el módulo AD del examen de <strong>mayo de 2023</strong> (medida con interrupción y variable global compartida).</p>", "contexto": "<ul><li>PBCLK = 5&nbsp;MHz, prescaler del Timer 3 a 1:1.</li><li>AD1CON3 = 0x0200 (SAMC = 2, ADCS = 0).</li><li>Lectura del conversor: 0 a 1023 (10 bits).</li><li><code>med</code> y <code>ref</code> son <code>volatile float</code> compartidas entre la ISR y el main.</li></ul>", "preguntas": [{"texto": "¿Qué configura exactamente la escritura AD1CHS = 0x00050000?", "opciones": ["Selecciona los canales AN5 y AN0 para medirlos a la vez", "CH0SA = 5: la entrada positiva del mux A es AN5, y CH0NA = 0: la negativa es Vref- (0 V)", "El canal AN5 se mide en modo diferencial respecto a AN1", "SMPI = 5: se generan interrupciones cada 5 conversiones"], "correcta": 1, "explicacion": "<p>El 5 cae en los bits 19-16, que son el campo <strong>CH0SA</strong> (canal de la entrada positiva del mux A): 0101 = AN5, el potenciómetro. El bit 23 (<strong>CH0NA</strong>) queda a 0: la entrada negativa es V<sub>REFL</sub> (0&nbsp;V), o sea, medida unipolar. Con un solo sample&amp;hold no se pueden medir dos canales «a la vez»: por eso la ISR alterna CH0SA entre 5 y 0 en cada conversión. SMPI está en AD1CON2, no en AD1CHS.</p>"}, {"texto": "¿Qué valor hay que cargar en PR3 para que el Timer 3 dispare una conversión cada 1 ms (prescaler 1:1)?", "opciones": ["5000", "1249", "4999", "19530"], "correcta": 2, "explicacion": "<p>PR3 = T·f<sub>PBCLK</sub>/prescaler − 1 = 1&nbsp;ms · 5&nbsp;MHz − 1 = 5000 − 1 = <strong>4999</strong>. Ojo con olvidar el −1: el temporizador cuenta desde 0, así que cuenta PR3 + 1 ciclos por periodo. 1249 sería 0,25&nbsp;ms y 19530 corresponde a ≈1&nbsp;s con prescaler 1:256.</p>"}, {"texto": "¿Por qué getTemp() copia la variable med entre asm(\"di\") y asm(\"ei\") en lugar de usarla directamente?", "opciones": ["Para que la ISR del ADC no se pierda mientras se ejecuta el return", "Porque med es un float de 4 bytes: si la ISR lo escribe a mitad de la lectura del main, la copia quedaría corrupta; di/ei garantiza una lectura atómica", "Porque el compilador optimiza las variables volatile y di/ei lo impide", "Es solo una buena costumbre: en el PIC32 las lecturas de float ya son atómicas"], "correcta": 1, "explicacion": "<p><code>med</code> la escribe la ISR y la lee el main: es un <strong>dato compartido</strong>. Un <code>float</code> ocupa 4 bytes y su lectura puede necesitar varias instrucciones; si la interrupción salta en medio y reescribe la variable, el main se llevaría una mezcla de valor viejo y nuevo. La pareja <code>asm(\"di\")</code>/<code>asm(\"ei\")</code> corta las interrupciones solo durante la copia local (lo mínimo imprescindible). El <code>volatile</code> evita además que el compilador cachee la variable en un registro. En la ISR no hace falta proteger nada: no puede interrumpirse a sí misma.</p>"}, {"texto": "Se pide añadir una histéresis de 1 °C al control todo/nada (como en el examen del horno de mayo 2022). ¿Cuál es la lógica correcta?", "opciones": ["Encender si getTemp() < getRef() y apagar si getTemp() > getRef() + 1", "Encender si getTemp() < getRef() − 1, apagar si getTemp() > getRef() + 1, y dentro de la banda mantener el estado anterior", "Encender si getTemp() < getRef() + 1 y apagar si getTemp() < getRef() − 1", "Esperar 1 segundo entre cada cambio de estado del ventilador"], "correcta": 1, "explicacion": "<p>La histéresis crea una <strong>banda muerta</strong> alrededor de la consigna: se enciende la calefacción cuando la temperatura cae por debajo de <code>ref − H</code> y se apaga cuando sube por encima de <code>ref + H</code>; entre medias <strong>no se cambia nada</strong> (se mantiene el estado). Así el ventilador no «traquetea» encendiéndose y apagándose con el ruido de la medida cuando la temperatura ronda la referencia. Es exactamente el control todo/nada con histéresis del problema del horno (mayo 2022, con H = 0,5&nbsp;°C).</p>"}]};

MPI.contenidoTemas["ejemplo-integrador-termostato"] = {
  titulo: "Ejemplo integrador: el termostato",
  html: "<header class=\"tema-cab\">\n<span class=\"tema-num\">Tema 12</span>\n<h1>Ejemplo integrador: el termostato</h1>\n<p class=\"tema-lead\">Un problema completo resuelto de principio a fin, como en el examen: el <strong>termostato de clase</strong>. Verás cómo trocearlo en drivers y un main, y cómo encajan el PWM del ventilador (<code class=\"reg\">OC4CON</code>), el conversor A/D con dos canales (<code class=\"reg\">AD1CHS</code>) y las interrupciones del Timer 3, todo con el orden canónico de inicialización del PIC32MX230F064D.</p>\n</header>\n<section>\n<h2>12.1 El problema: el termostato de clase</h2>\n<p>Este tema cierra la asignatura con un <strong>problema libre</strong>, del estilo de los que caen en el examen: el termostato que hay colgado en la pared de clase. Solo vamos a implementar el modo <em>calor</em>: cuando la temperatura medida esté por debajo de la referencia fijada por el usuario, el ventilador soplará aire caliente a la velocidad seleccionada. El primer paso de cualquier problema de este tipo es hacer inventario del hardware y asignar pines (en el examen suelen venir dados; aquí nos los inventamos mirando la tabla de pines del datasheet):</p>\n<ul><li><strong>LED de modo calor</strong> en <code>RC3</code>, activo a nivel alto: encendido mientras el termostato está calentando.</li><li><strong>Interruptores de velocidad</strong> del ventilador en <code>RC0</code>, <code>RC1</code> y <code>RC2</code> (baja, media y alta), todos activos a nivel alto.</li><li><strong>Referencia de temperatura</strong> en un potenciómetro conectado a <code>RB3/AN5</code>: todo su recorrido (0 a 3,3&nbsp;V) cubre de 8 a 30&nbsp;°C.</li><li><strong>Sensor de temperatura analógico</strong> en <code>RA0/AN0</code>: mide entre 2 y 50&nbsp;°C, proporcional a la tensión.</li><li><strong>Motor del ventilador</strong> controlado por una señal <strong>PWM de 20&nbsp;kHz</strong> generada con el <strong>Output Compare 4</strong> en <code>RC6</code>.</li></ul>\n<div class=\"nota\">Consejo de examen: no hace falta leerlo todo en detalle desde el principio. Fíjate en <em>cuándo</em> se necesita cada dato del enunciado: la fórmula del sensor no se usa hasta que llegas al driver del AD. Lee el enunciado por pasadas: una rápida para ver la estructura y otra en detalle cuando ataques cada apartado.</div>\n</section>\n<section>\n<h2>12.2 ¿Por dónde empiezo? Drivers y main</h2>\n<p>Ante un problema con varios periféricos hay dos estrategias razonables, y conviene decidir conscientemente cuál seguir:</p>\n<ul><li><strong>Empezar por el main:</strong> empiezas por algo sencillo y te <em>inventas</em> las funciones que necesitas de los drivers (sus cabeceras), para implementarlas después. Riesgo: un driver corto y fácil (el OC, por ejemplo) puede valer los mismos puntos y quedarse sin hacer por falta de tiempo.</li><li><strong>Empezar por los periféricos:</strong> si ves claras las funciones, aseguras puntos antes de pelearte con la lógica de integración del main. Riesgo: si aún no sabes qué funciones necesitas, acabas escribiendo código que luego hay que retocar. Un esquema inicial del problema ayuda a identificar las funciones necesarias.</li></ul>\n<p>En clase se votó y se decidió el orden: <strong>primero el OC, después el main y por último el AD</strong>. Sea cual sea el orden, la arquitectura es siempre la misma: cada periférico se encapsula en un <strong>driver</strong> con una función de inicialización (que se llama una sola vez antes del <code>while</code>) y funciones para cambiar algo (<em>setters</em>) o consultar algo (<em>getters</em>). En el termostato: <code>iniOC()</code> y <code>velVent()</code> para el ventilador; <code>iniAD()</code>, <code>getTemp()</code> y <code>getRef()</code> para las medidas analógicas.</p>\n<div class=\"nota\">Regla de oro de los drivers: en sus inicializaciones usan <strong>máscaras</strong> (<code>|=</code>, <code>&amp;= ~</code>) para tocar solo sus bits y no borrar lo que se configura fuera. Y cuando el enunciado deja algo a tu elección: <strong>hazlo simple</strong>, o elige la opción que veas más clara.</div>\n</section>\n<section>\n<h2>12.3 Driver del ventilador: OC4 + Timer 2 a 20 kHz</h2>\n<p>El motor del ventilador se controla con una señal PWM de <strong>20&nbsp;kHz</strong> (por encima del rango audible, para que no silbe) y debe tener <strong>cuatro velocidades</strong>: apagado (0/3), baja (1/3), media (2/3) y alta (3/3) del factor de servicio. El periodo de la onda es <em>T</em> = 1/20&nbsp;kHz = 50&nbsp;µs, que con PBCLK a 5&nbsp;MHz y prescaler 1:1 cabe de sobra en el Timer&nbsp;2:</p>\n<p>Valor del registro de periodo del Timer 2 para generar la base de tiempos del PWM (el −1 es porque el temporizador cuenta desde 0):</p><math class=\"formula\" xmlns=\"http://www.w3.org/1998/Math/MathML\" display=\"block\"><mrow><msub><mi>PR</mi><mn>2</mn></msub><mo>=</mo><mfrac><mrow><mi>T</mi><mo>·</mo><msub><mi>f</mi><mtext>PBCLK</mtext></msub></mrow><mtext>prescaler</mtext></mfrac><mo>−</mo><mn>1</mn><mo>=</mo><mfrac><mrow><mn>50</mn><mtext>&#160;µs</mtext><mo>·</mo><mn>5</mn><mtext>&#160;MHz</mtext></mrow><mn>1</mn></mfrac><mo>−</mo><mn>1</mn><mo>=</mo><mn>249</mn></mrow></math>\n<p>La salida OC4 no está cableada a ningún pin fijo: hay que <strong>remapearla</strong>. Según la tabla <em>OUTPUT PIN SELECTION</em> del datasheet, <code>RC6</code> (pin remapeable <code>RPC6</code>) pertenece al grupo en el que el valor <strong>5</strong> selecciona <strong>OC4</strong>, así que basta con escribir <code>RPC6R = 5</code>… previa secuencia de desbloqueo <code class=\"reg\">SYSKEY</code>, porque los registros de remapeo están protegidos contra escrituras accidentales. El orden canónico del driver es: remapeo → OC (apagado, cargar <code class=\"reg\">OC4R</code>/<code class=\"reg\">OC4RS</code>, encender) → Timer&nbsp;2 (siempre después del OC):</p>\n<p>Driver <code>OC.c</code> del ventilador, tal y como se construyó en clase. Fíjate en la máscara de <code>TRISC</code> y en que <code>velVent()</code> solo escribe en <code class=\"reg\">OC4RS</code>, que es el registro de actualización segura del duty:</p><pre><code class=\"lang-c\">#include &lt;xc.h&gt;\n#include &lt;stdint.h&gt;\n#include \"OC.h\"\n\n#define PERIODO 249           // 20 kHz -&gt; T = 50 us con PBCLK = 5 MHz\n\nvoid iniOC(void)\n{\n    TRISC &amp;= ~(1 &lt;&lt; 6);       // RC6 como salida (mascara: no toco el resto)\n\n    SYSKEY = 0xAA996655;      // Secuencia de desbloqueo del remapeo\n    SYSKEY = 0x556699AA;\n    RPC6R = 5;                // OC4 conectado a RC6\n    SYSKEY = 0x1CA11CA1;      // Se vuelve a bloquear\n\n    OC4CON = 0;               // OC apagado mientras se configura\n    OC4R = 0;                 // Ancho del primer pulso: ventilador parado\n    OC4RS = 0;                // Anchos siguientes: tambien parado\n    OC4CON = 0x8006;          // ON, modo PWM sin deteccion de faltas\n\n    T2CON = 0;                // El Timer 2 SIEMPRE despues del OC\n    TMR2 = 0;\n    PR2 = PERIODO;            // Periodo de 50 us\n    T2CON = 0x8000;           // T2 ON, prescaler 1:1\n}\n\nvoid velVent(uint8_t intensidad)\n{\n    // intensidad = 0 (parado), 1 (baja), 2 (media), 3 (alta)\n    OC4RS = (intensidad * PERIODO) / 3;\n}</code></pre>\n<div class=\"mpi-mount\" data-componente=\"visor-bits\" data-config='{\"registro\": \"OC4CON\", \"valor\": \"0x8006\"}'></div>\n<div class=\"mpi-mount\" data-componente=\"visor-bits\" data-config='{\"registro\": \"T2CON\", \"valor\": \"0x8000\"}'></div>\n<div class=\"nota\">Para seguir pensando, como se pidió en clase: ¿qué cambiarías para <strong>apagar el periférico cuando no hace falta</strong> (bit ON de <code class=\"reg\">OC4CON</code> y del Timer)? ¿Y para que el driver pueda <strong>elegir entre Timer 2 o Timer 3</strong> como base de tiempos? (pista: bit OCTSEL de <code class=\"reg\">OC4CON</code>).</div>\n</section>\n<section>\n<h2>12.4 El main: el que reparte el juego</h2>\n<p>El <code>main</code> sigue el orden canónico de arranque: primero <code class=\"reg\">LAT</code> a 0 (que las salidas arranquen apagadas, como recomienda Microchip), después los <code class=\"reg\">TRIS</code> (todo salida salvo los interruptores) junto con el <code class=\"reg\">ANSELC</code> que pasa a digital los pines analógicos que usa el propio main, después las inicializaciones de los drivers (cada uno retoca <em>sus</em> <code class=\"reg\">TRIS</code>/<code class=\"reg\">ANSEL</code> con máscaras, así el main no tiene que pensar en ellos) y, <strong>al final</strong>, el modo multivector y la habilitación global de interrupciones. Solo entonces se entra en el bucle de <em>scan</em>:</p>\n<p><code>main.c</code> del termostato. El bucle separa claramente <strong>entradas → lógica → salidas</strong>: primero se leen los interruptores y luego se decide. Si hay varios interruptores a 1, el bucle <code>for</code> se queda con el de mayor prioridad (el último que encuentre a 1):</p><pre><code class=\"lang-c\">#include &lt;xc.h&gt;\n#include &lt;stdint.h&gt;\n#include \"OC.h\"\n#include \"AD.h\"\n\nint main(void)\n{\n    uint8_t vel, i;\n\n    LATA = 0;                  // Salidas a 0 en el arranque\n    LATB = 0;\n    LATC = 0;                  // LED de calor apagado (activo a nivel alto)\n\n    TRISA = 0;                 // Todo como salida...\n    TRISB = 0;\n    TRISC = 0x7;               // ...excepto los interruptores RC0-RC2\n    ANSELC &amp;= ~0xF;            // RC0-RC2 (AN6-AN8) y RC3 (AN12) a digital:\n                               // si no, los interruptores se leerian a 0\n\n    iniOC();                   // El driver ajusta los TRIS que necesita\n    iniAD();                   // Idem con TRIS y ANSEL de AN0 y AN5\n\n    INTCONbits.MVEC = 1;       // Modo multivector\n    asm(\"ei\");                 // Interrupciones globales: SIEMPRE al final\n\n    while (1) {\n        // 1) Entradas: se leen ANTES de la logica\n        vel = 0;\n        for (i = 0; i &lt; 3; i++) {\n            if ((PORTC &gt;&gt; i) &amp; 1) {\n                vel = i + 1;   // 1 = baja, 2 = media, 3 = alta\n            }\n        }\n        // 2) Logica y salidas (control todo/nada)\n        if (vel != 0 &amp;&amp; getTemp() &lt; getRef()) {\n            velVent(vel);          // Ventilador a la velocidad elegida\n            LATCSET = 1 &lt;&lt; 3;      // LED de calor encendido\n        } else if (getTemp() &gt; getRef() || vel == 0) {\n            velVent(0);            // Ventilador parado\n            LATCCLR = 1 &lt;&lt; 3;      // LED de calor apagado\n        }\n    }\n}</code></pre>\n<ul><li><code>TRISC = 0x7</code> configura los tres interruptores de golpe: no hace falta un <code>(1&lt;&lt;i)</code> por cada uno.</li><li><code>ANSELC &amp;= ~0xF</code> es imprescindible: en el encapsulado de 44 pines RC0–RC2 comparten pin con <strong>AN6–AN8</strong> (y RC3 con AN12). Tras el reset <code class=\"reg\">ANSELC</code> vale 0x000F (modo analógico) y la lectura digital de un pin analógico devuelve siempre 0: los interruptores no funcionarían.</li><li><code>LATCSET</code> y <code>LATCCLR</code> escriben el LED de forma <strong>atómica</strong>, sin leer-modificar-escribir.</li><li>El control es <strong>todo/nada</strong>: si la temperatura queda exactamente igual a la referencia no se cambia nada y se mantiene el estado anterior. Con histéresis esta banda muerta se hace explícita (sección 12.6).</li><li><code>getTemp()</code> y <code>getRef()</code> son getters del driver del AD: el main no sabe (ni le importa) que por debajo hay interrupciones.</li></ul>\n<div class=\"nota\">El <code class=\"reg\">ANSEL</code> se comprueba siempre contra la tabla de pines del datasheet: todo pin con función analógica arranca en modo analógico tras el reset y su lectura digital devuelve 0. AN0 y AN5 los configura su propio driver, pero los interruptores RC0–RC2 son además <strong>AN6–AN8</strong>, así que el main debe pasarlos a digital con <code class=\"reg\">ANSELC</code>. El LED en RC3 (AN12) funcionaría igualmente como salida digital (el ANSEL solo desconecta el buffer de entrada), pero se pasa a digital por uniformidad; RC6 no tiene función analógica.</div>\n</section>\n<section>\n<h2>12.5 Driver del AD: dos canales alternados con Timer 3</h2>\n<p>Hay que medir <strong>dos magnitudes analógicas</strong> (referencia en AN5 y sensor en AN0) con un conversor que solo tiene un <em>sample&amp;hold</em>. Las dos opciones razonables son el <strong>escaneo automático</strong> (CSCNA + <code class=\"reg\">AD1CSSL</code>) o ir <strong>alternando el canal a mano</strong> reescribiendo <code class=\"reg\">AD1CHS</code> en cada interrupción. En clase no se pudo votar, así que nos lo inventamos: <strong>modo continuo controlado por el Timer&nbsp;3</strong> (SSRC&nbsp;=&nbsp;2, ASAM&nbsp;=&nbsp;1), una conversión por interrupción (SMPI&nbsp;=&nbsp;0) cada 1&nbsp;ms, y la ISR alterna el canal. Las conversiones a unidades físicas salen de interpolar linealmente el rango de cada sensor (si no ves estas fórmulas, repasa el tema del AD):</p>\n<p>Referencia de temperatura: el potenciómetro cubre de 8 a 30&nbsp;°C en todo su recorrido (lectura de 0 a 1023):</p><math class=\"formula\" xmlns=\"http://www.w3.org/1998/Math/MathML\" display=\"block\"><mrow><msub><mi>T</mi><mtext>ref</mtext></msub><mo>=</mo><mn>8</mn><mo>+</mo><mfrac><mrow><msub><mtext>medida</mtext><mtext>AD</mtext></msub><mo>·</mo><mo stretchy=\"false\">(</mo><mn>30</mn><mo>−</mo><mn>8</mn><mo stretchy=\"false\">)</mo></mrow><mn>1023</mn></mfrac></mrow></math>\n<p>Temperatura medida: el sensor entrega de 2 a 50&nbsp;°C proporcional a la tensión:</p><math class=\"formula\" xmlns=\"http://www.w3.org/1998/Math/MathML\" display=\"block\"><mrow><msub><mi>T</mi><mtext>med</mtext></msub><mo>=</mo><mn>2</mn><mo>+</mo><mfrac><mrow><msub><mtext>medida</mtext><mtext>AD</mtext></msub><mo>·</mo><mo stretchy=\"false\">(</mo><mn>50</mn><mo>−</mo><mn>2</mn><mo stretchy=\"false\">)</mo></mrow><mn>1023</mn></mfrac></mrow></math>\n<p>El Timer 3 dispara una conversión cada milisegundo (prescaler 1:1):</p><math class=\"formula\" xmlns=\"http://www.w3.org/1998/Math/MathML\" display=\"block\"><mrow><msub><mi>PR</mi><mn>3</mn></msub><mo>=</mo><mn>1</mn><mtext>&#160;ms</mtext><mo>·</mo><mn>5</mn><mtext>&#160;MHz</mtext><mo>−</mo><mn>1</mn><mo>=</mo><mn>4999</mn></mrow></math>\n<p>Driver <code>AD.c</code> completo: inicialización en orden canónico (ADC configurado y encendido <strong>antes</strong> de arrancar el Timer&nbsp;3 que lo dispara), ISR por el vector 23 que alterna AN5/AN0, y getters que convierten a grados:</p><pre><code class=\"lang-c\">#include &lt;xc.h&gt;\n#include &lt;stdint.h&gt;\n#include \"AD.h\"\n\nstatic volatile float med = 0, ref = 0;   // Compartidas con la ISR\n\nvoid iniAD(void)\n{\n    TRISA |= 1;               // RA0/AN0 como entrada (sensor)...\n    ANSELA |= 1;              // ...y analogica (aseguro con mascara)\n    TRISB |= (1 &lt;&lt; 3);        // RB3/AN5 como entrada (potenciometro)...\n    ANSELB |= (1 &lt;&lt; 3);       // ...y analogica\n\n    AD1CON1 = 0;              // ADC apagado mientras se configura\n    AD1CON2 = 0;              // SMPI = 0: interrupcion por cada conversion\n    AD1CON3 = 0x0200;         // SAMC = 2, ADCS = 0 (TAD = 2*TPBCLK)\n    AD1CHS = 0x00050000;      // CH0SA = AN5, CH0NA = Vref- (0 V)\n    AD1CON1bits.SSRC = 2;     // Disparo por fin de cuenta del Timer 3\n    AD1CON1bits.ASAM = 1;     // Muestreo automatico tras cada conversion\n\n    IFS0bits.AD1IF = 0;       // Borro el flag...\n    IEC0bits.AD1IE = 1;       // ...habilito la interrupcion...\n    IPC5bits.AD1IP = 2;       // ...prioridad 2...\n    IPC5bits.AD1IS = 0;       // ...subprioridad 0\n    AD1CON1bits.ON = 1;       // Arranco el ADC\n\n    T3CON = 0;                // El Timer 3 despues del ADC\n    TMR3 = 0;\n    PR3 = 4999;               // Una conversion cada 1 ms\n    T3CON = 0x8000;           // T3 ON, prescaler 1:1\n}\n\n__attribute__((vector(23), interrupt(IPL2SOFT), nomips16))\nvoid InterrupcionADC(void)\n{\n    static uint8_t canal = 0;     // 0: toca leer AN5, 1: toca leer AN0\n\n    IFS0bits.AD1IF = 0;           // Se borra el flag\n\n    if (canal) {\n        med = ADC1BUF0;           // Medida del sensor (AN0)\n        AD1CHS = 0x00050000;      // Siguiente conversion: AN5\n    } else {\n        ref = ADC1BUF0;           // Medida del potenciometro (AN5)\n        AD1CHS = 0x00000000;      // Siguiente conversion: AN0\n    }\n    canal ^= 1;\n}\n\nfloat getTemp(void)\n{\n    float copia;\n    asm(\"di\");                    // Copia atomica de la variable\n    copia = med;                  // compartida con la ISR\n    asm(\"ei\");\n    return 2 + copia * 48.0 / 1023;    // Sensor: 2...50 C\n}\n\nfloat getRef(void)\n{\n    float copia;\n    asm(\"di\");\n    copia = ref;\n    asm(\"ei\");\n    return 8 + copia * 22.0 / 1023;    // Potenciometro: 8...30 C\n}</code></pre>\n<div class=\"mpi-mount\" data-componente=\"visor-bits\" data-config='{\"registro\": \"AD1CON1\", \"valor\": \"0x8044\"}'></div>\n<div class=\"mpi-mount\" data-componente=\"visor-bits\" data-config='{\"registro\": \"AD1CHS\", \"valor\": \"0x00050000\"}'></div>\n<div class=\"mpi-mount\" data-componente=\"visor-bits\" data-config='{\"registro\": \"AD1CON3\", \"valor\": \"0x0200\"}'></div>\n<div class=\"nota\"><code>med</code> y <code>ref</code> son <code>float</code> de 4 bytes compartidos con la ISR: se declaran <strong><code>volatile</code></strong> y en el main (los getters) se leen con una copia local protegida por <strong><code>asm(\"di\")</code> / <code>asm(\"ei\")</code></strong>. Si la interrupción escribiera a mitad de la lectura, la copia podría quedar corrupta. En la ISR no hace falta proteger: una interrupción no se interrumpe a sí misma.</div>\n</section>\n<section>\n<h2>12.6 Variantes: lo que te pueden preguntar</h2>\n<p>En el examen es habitual que, sobre una base como esta, se pidan <strong>cambios</strong>. Estas son las variantes que se plantearon en clase (piensa cómo resolverías cada una antes de mirar la pista):</p>\n<ul><li><strong>Meterle UART y quitar la referencia del AD:</strong> la consigna llegaría por <code>U1RX</code> (interrupción de recepción, vector 32) y desaparece AN5: el AD se queda con un solo canal fijo y la ISR ya no alterna.</li><li><strong>Enviar la medida por UART:</strong> un <code>sprintf</code> + envío en el main (o por interrupción de TX) cada vez que cambie la temperatura.</li><li><strong>Histéresis de 1&nbsp;°C:</strong> encender si <code>getTemp() &lt; getRef() − 1</code> y apagar si <code>getTemp() &gt; getRef() + 1</code>; dentro de la banda se mantiene el estado. Es el mismo control todo/nada con histéresis del examen del horno (mayo 2022).</li><li><strong>Medir y actuar cada 5 minutos:</strong> en vez de decidir en cada vuelta del bucle, un temporizador (o un contador de ticks) marca cuándo toca ejecutar la lógica.</li><li><strong>Cambiar el AD a escaneo:</strong> CSCNA&nbsp;=&nbsp;1, <code class=\"reg\">AD1CSSL</code>&nbsp;=&nbsp;0x21 (AN0 y AN5), SMPI&nbsp;=&nbsp;1 (interrupción cada 2 conversiones): los resultados llegan en orden de canal en <code class=\"reg\">ADC1BUF0</code> y <code class=\"reg\">ADC1BUF1</code> y la ISR ya no reescribe <code class=\"reg\">AD1CHS</code>.</li><li><strong>Medir 5 veces y promediar:</strong> SMPI&nbsp;=&nbsp;4 sobre el mismo canal y media de <code class=\"reg\">ADC1BUF0</code>…<code class=\"reg\">ADC1BUF4</code> en la ISR, para filtrar ruido.</li></ul>\n<div class=\"nota\">Fíjate en la moraleja final del tema: cada parte de la información del enunciado se ha necesitado <em>en un momento concreto</em> del desarrollo. Aprende a leer enunciados largos por capas: estructura primero, detalle cuando toque.</div>\n</section>\n<section>\n<h2>Ejercicios de diseño de drivers</h2>\n<div class=\"mpi-mount\" data-componente=\"ejercicio-driver\" data-config='{\"ref\": \"ejemplo-integrador-termostato-1\"}'></div>\n<div class=\"mpi-mount\" data-componente=\"ejercicio-driver\" data-config='{\"ref\": \"ejemplo-integrador-termostato-2\"}'></div>\n</section>\n<section class=\"tema-cierre\"><h2>Resumen</h2><ul>\n<li>Un problema «libre» se ataca <strong>troceándolo en drivers + main</strong>: cada driver tiene una inicialización (que se llama una sola vez antes del <code>while</code> y usa <strong>máscaras</strong>) más <em>setters</em>/<em>getters</em>. Decide conscientemente si empiezas por el main o por los periféricos.</li>\n<li>Ventilador: PWM de 20&nbsp;kHz con <strong>OC4 remapeado a RC6</strong> (<code>RPC6R = 5</code> tras la secuencia SYSKEY), <code class=\"reg\">OC4CON</code> = 0x8006 y Timer 2 con PR2 = 50&nbsp;µs·5&nbsp;MHz − 1 = 249. El OC se configura <strong>antes</strong> que su timer, y el duty solo se cambia por <code class=\"reg\">OC4RS</code>.</li>\n<li>Main canónico: <code class=\"reg\">LAT</code> = 0 → <code class=\"reg\">TRIS</code> y <code class=\"reg\">ANSELC</code> a digital (¡en este encapsulado RC0–RC2 son AN6–AN8!) → inicializaciones de drivers → <code>INTCONbits.MVEC = 1</code> + <code>asm(\"ei\")</code> al final → bucle con <strong>entradas → lógica → salidas</strong>.</li>\n<li>AD con dos canales y un solo S&amp;H: modo continuo disparado por el <strong>Timer 3</strong> (SSRC = 2, ASAM = 1, PR3 = 4999 para 1&nbsp;ms) y la ISR (vector 23) <strong>alterna CH0SA</strong> entre AN5 y AN0 reescribiendo <code class=\"reg\">AD1CHS</code>.</li>\n<li>Conversión a unidades físicas por interpolación lineal: T = T<sub>min</sub> + medida·(T<sub>max</sub> − T<sub>min</sub>)/1023; con el potenciómetro 8…30&nbsp;°C y el sensor 2…50&nbsp;°C.</li>\n<li>Datos compartidos con la ISR: <code>volatile</code> + copia local protegida con <code>asm(\"di\")</code>/<code>asm(\"ei\")</code> en el main; la ISR accede directo.</li>\n<li>Variantes típicas de examen sobre esta base: UART para consigna/medida, <strong>histéresis</strong> (encender bajo ref − H, apagar sobre ref + H), actuar cada N minutos, AD por escaneo (CSCNA + <code class=\"reg\">AD1CSSL</code>) o promediado con SMPI.</li>\n</ul></section>"
};
