/*
 * Contenido del tema "Arquitectura hardware y software" (Tema 5).
 * GENERADO por herramientas/spec_a_tema.py desde datos/_specs/arquitectura-mips.json — no editar a mano; edita el spec y regenera.
 */
window.MPI = window.MPI || {};
MPI.contenidoTemas = MPI.contenidoTemas || {};
MPI.ejercicios = MPI.ejercicios || {};
MPI.registros = MPI.registros || {};

MPI.ejercicios["arquitectura-mips-1"] = {"titulo": "Mapa de memoria y acceso a SFR por punteros (Examen Ordinario 2026)", "enunciado": "<p>En el problema del <em>robot autónomo</em> del Examen Ordinario de 2026 no se podía usar el nombre del registro <code class=\"reg\">ADC1BUFn</code>: el acceso a los resultados del conversor A/D debía realizarse <strong>mediante punteros a memoria</strong>. El enunciado indicaba que la dirección base del primer buffer, <code class=\"reg\">ADC1BUF0</code>, es <code>0xBF809070</code>, y que los siguientes buffers se obtienen sumando <code>0x10</code> (16 bytes) por cada canal: <code class=\"reg\">ADC1BUF1</code> está en <code>0xBF809080</code>, etcétera.</p>", "contexto": "<ul><li>El PIC32MX230F064D trabaja por defecto en el segmento <strong>KSEG1</strong> (no cacheable).</li><li>Bloques en KSEG1: RAM en <code>0xA0000000</code>, FLASH de programa en <code>0xBD000000</code>, SFRs en <code>0xBF800000</code> (1 MB), Boot Flash en <code>0xBFC00000</code>.</li><li><code class=\"reg\">ADC1BUF0</code> = <code>0xBF809070</code>; separación entre buffers = <code>0x10</code> bytes.</li></ul>", "preguntas": [{"texto": "¿En qué bloque del mapa de memoria está la dirección 0xBF809070?", "opciones": ["En la RAM, porque es donde se guardan los resultados de las conversiones", "En la FLASH de programa, porque el ADC forma parte del firmware", "En el bloque de SFRs de KSEG1: 1 MB reservado a partir de 0xBF800000", "En la Boot Flash, junto a los registros de configuración del micro"], "correcta": 2, "explicacion": "<p>Las direcciones <code>0xBF80xxxx</code> caen dentro del megabyte reservado para los <strong>SFRs</strong> (registros de los periféricos), que empieza en <code>0xBF800000</code> en KSEG1. La RAM empieza en <code>0xA0000000</code>, la FLASH de programa en <code>0xBD000000</code> y la Boot Flash en <code>0xBFC00000</code>.</p>"}, {"texto": "¿Cuál es la dirección de ADC1BUF2?", "opciones": ["0xBF809074", "0xBF809080", "0xBF809090", "0xBF809270"], "correcta": 2, "explicacion": "<p>Cada buffer está separado <code>0x10</code> bytes: <code>dir(ADC1BUFn) = 0xBF809070 + n·0x10</code>. Para n = 2: <code>0xBF809070 + 0x20 = 0xBF809090</code>. La opción <code>0xBF809074</code> sería sumar solo 4 bytes (una palabra), que es la separación entre posiciones consecutivas de 32 bits, no entre buffers.</p>"}, {"texto": "¿Cuál es la forma correcta de declarar el puntero para leer ADC1BUF0?", "opciones": ["uint32_t *p = (uint32_t *)0xBF809070;", "volatile uint32_t *p = (volatile uint32_t *)0xBF809070;", "uint32_t p = 0xBF809070;", "volatile uint32_t p = *0xBF809070;"], "correcta": 1, "explicacion": "<p>Hace falta un <strong>puntero</strong> (no un entero) inicializado con la dirección, y el calificador <code>volatile</code> es imprescindible: el contenido del registro lo cambia el <em>hardware</em> sin que el compilador lo sepa, y sin <code>volatile</code> el optimizador podría leerlo una sola vez y reutilizar el valor antiguo. Las opciones 3 y 4 ni siquiera declaran un puntero.</p>"}, {"texto": "¿Por qué los SFRs están en KSEG1 y no en KSEG0?", "opciones": ["Porque KSEG1 no es cacheable: un SFR puede cambiar en cualquier momento y una copia en caché quedaría obsoleta", "Porque KSEG0 es de solo lectura y los SFRs hay que escribirlos", "Porque KSEG1 tiene un bus más rápido dedicado a periféricos", "Porque los SFRs no caben en KSEG0, que es más pequeño"], "correcta": 0, "explicacion": "<p>La única diferencia entre ambos segmentos es que <strong>KSEG0 es cacheable y KSEG1 no</strong>. Un registro de periférico puede cambiar en cualquier momento (lo modifica el hardware), así que cachearlo daría lecturas erróneas: por eso el bloque de SFRs solo existe en KSEG1. En el PIC32MX230F064D, que no tiene caché, se usa KSEG1 por defecto para todo.</p>"}]};
MPI.ejercicios["arquitectura-mips-2"] = {"titulo": "Diseño de un módulo/driver (estilo examen)", "enunciado": "<p>El Examen Ordinario de 2026 pedía un driver del sensor acústico de un mecedor automático en dos archivos, <code>sensor.c</code> y <code>sensor.h</code>, con las funciones <code>void iniciaSensor(uint8_t pinB)</code> y <code>uint8_t getLlanto(void)</code>, y una variable interna <code>hayLlanto</code> que vale 1 cuando se detecta llanto. El programa principal, <code>main.c</code>, debe usar el driver para decidir si activa el motor de balanceo.</p>", "contexto": "<ul><li>El driver consta de <code>sensor.c</code> (definiciones) y <code>sensor.h</code> (declaraciones públicas).</li><li><code>main.c</code> solo conoce lo que aparece en <code>sensor.h</code>.</li><li>Regla del curso: en un driver no puede haber variables externas; se usan <em>setters</em> y <em>getters</em>.</li></ul>", "preguntas": [{"texto": "¿Dónde debe declararse la cabecera uint8_t getLlanto(void); para que main.c pueda usarla?", "opciones": ["En sensor.c, justo encima de su definición", "En sensor.h, acompañada de un breve comentario que explique cómo usarla", "En main.c, antes de la función main()", "En xc.h, que es donde van todas las cabeceras del proyecto"], "correcta": 1, "explicacion": "<p>Las funciones <strong>públicas</strong> de un driver se declaran en su <code>.h</code>, junto con una breve explicación para que un tercero sepa usarlas sin leer el código. <code>main.c</code> hace <code>#include \"sensor.h\"</code> y con eso conoce la cabecera. Declararla en <code>main.c</code> funcionaría, pero rompe la encapsulación del módulo y es justo lo que la metodología del curso prohíbe.</p>"}, {"texto": "¿Cómo debe gestionarse la variable hayLlanto del módulo?", "opciones": ["Declararla extern en sensor.h para que main.c la lea directamente", "Declararla static en sensor.c y acceder a ella desde fuera únicamente mediante el getter getLlanto()", "Declararla global en main.c y pasarla por puntero a iniciaSensor()", "Sustituirla por un #define HAYLLANTO en sensor.h"], "correcta": 1, "explicacion": "<p>Regla del curso: <strong>no puede haber variables externas</strong>. Las variables globales del módulo se declaran <code>static</code> en el <code>.c</code> (solo visibles dentro del módulo) y se establecen con <em>setters</em> y se leen con <em>getters</em>. Así, si mañana cambia cómo se detecta el llanto, <code>main.c</code> no se entera: sigue llamando a <code>getLlanto()</code>. Un <code>#define</code> no sirve porque su valor no puede cambiar en ejecución.</p>"}, {"texto": "¿Qué directivas #include son correctas al principio de sensor.c?", "opciones": ["<code>#include \"xc.h\"</code> y <code>#include \"sensor.h\"</code>: las comillas valen para todo", "<code>#include &lt;xc.h&gt;</code> y <code>#include &lt;sensor.h&gt;</code>: los ángulos valen para todo", "<code>#include &lt;xc.h&gt;</code> con ángulos por ser librería de Microchip, y <code>#include \"sensor.h\"</code> con comillas por ser un archivo nuestro", "Ninguna: sensor.c no debe incluir sensor.h porque sería una inclusión circular"], "correcta": 2, "explicacion": "<p>La convención es: <strong>ángulos <code>&lt;&gt;</code> para librerías</strong> del sistema o de Microchip (<code>xc.h</code>, <code>stdint.h</code>) y <strong>comillas <code>\"\"</code> para archivos nuestros</strong> (<code>sensor.h</code>). Además, <code>sensor.c</code> <em>sí</em> incluye su propio <code>.h</code>: así el compilador comprueba que las definiciones coinciden con las declaraciones públicas. No hay circularidad: un <code>.h</code> no incluye al <code>.c</code>.</p>"}, {"texto": "El driver necesita una función auxiliar de uso interno que no debe llamar nadie de fuera. ¿Cómo se trata?", "opciones": ["Se declara en sensor.h, pero con un comentario que diga 'no usar'", "Se declara y define solo en sensor.c (idealmente static): al no aparecer en el .h, es invisible para el resto del programa", "Debe llamarse igual que una pública pero con el prefijo priv_", "No puede existir: todas las funciones de un driver son públicas por definición"], "correcta": 1, "explicacion": "<p>Las funciones <strong>privadas</strong> se declaran y definen en el <code>.c</code> y no aparecen en el <code>.h</code>, de modo que ningún otro módulo puede llamarlas. Declararlas <code>static</code> refuerza esa privacidad a nivel de enlazado. Es lo que hace <code>initPin()</code> en el ejemplo <code>valorPinB</code> de este tema: <code>PinAsInput()</code> y <code>PinAsOutput()</code> la usan por dentro, pero no es parte de la interfaz pública.</p>"}]};

MPI.contenidoTemas["arquitectura-mips"] = {
  titulo: "Arquitectura hardware y software",
  html: "<header class=\"tema-cab\">\n<span class=\"tema-num\">Tema 5</span>\n<h1>Arquitectura hardware y software</h1>\n<p class=\"tema-lead\">El PIC32MX230F064D integra en un solo chip una CPU <strong>MIPS32 M4K</strong>, memoria FLASH, RAM y periféricos, todos colgados de un único mapa de memoria de 4&nbsp;GB. En este tema verás cómo es ese hardware por dentro (pipeline, <em>KSEG1</em>, alineación) y cómo organizar el software en <strong>módulos o drivers</strong>, que es exactamente lo que se pide en el examen.</p>\n</header>\n<section>\n<h2>5.1 El microcontrolador por dentro: CPU, memorias y periféricos</h2>\n<p>Un microcontrolador reúne en un único chip todo lo necesario para construir un sistema empotrado: la CPU, las memorias y los periféricos. En el PIC32MX230F064D estos bloques se comunican a través de una matriz de buses interna y comparten un único espacio de direcciones, de forma que para la CPU una variable, una instrucción o el registro de un periférico son simplemente posiciones de memoria.</p>\n<ul><li><strong>Memoria de programa:</strong> una memoria <em>no volátil</em> (no se borra al apagar el circuito). Antiguamente eran memorias ROM o EEPROM, pero ahora son <strong>FLASH</strong> por su capacidad de reprogramación.</li><li><strong>Memoria de datos RAM:</strong> aquí viven las <em>variables</em> y la <em>pila (stack)</em>. Es volátil: su contenido se pierde al quitar la alimentación.</li><li><strong>Periféricos:</strong> puertos de entrada y salida digitales, controlador USB 2.0, temporizadores, interfaces de comunicación (UART, SPI, I2C), conversores A/D, unidades de medida de carga/tiempo…</li></ul>\n<div class=\"nota\">La CPU del PIC32MX230F064D no la diseñó Microchip: es un núcleo (<em>core</em>) <strong>MIPS32 M4K</strong> licenciado por MIPS Technologies, una empresa dedicada a diseñar y licenciar núcleos de CPU. Núcleos MIPS se han usado en la PSP de Sony, en routers Linksys WRT54G o en reproductores Blu-ray.</div>\n</section>\n<section>\n<h2>5.2 La CPU: load/store con un pipeline de cinco etapas</h2>\n<p>El núcleo de ejecución (<em>Execution Core</em>) del M4K implementa una arquitectura de tipo <strong>load/store</strong>: todas las operaciones se realizan entre registros de la CPU y a la memoria solo se accede mediante instrucciones de carga y almacenamiento. Como recordará de Sistemas Digitales, la CPU está <strong>segmentada</strong> (<em>pipeline</em>) en <strong>cinco etapas</strong>, de manera que mientras una instrucción se ejecuta, la siguiente ya se está decodificando y otra más se está leyendo de memoria:</p>\n<ol><li><strong>Lectura (fetch):</strong> se lee la instrucción de la memoria de programa.</li><li><strong>Decodificación:</strong> se interpreta la instrucción y se leen los operandos del banco de registros.</li><li><strong>Ejecución:</strong> la ALU realiza la operación (suma/resta, lógica o desplazamiento) en un ciclo.</li><li><strong>Acceso a memoria:</strong> si la instrucción es de carga o almacenamiento, se accede a la memoria de datos.</li><li><strong>Escritura (write-back):</strong> el resultado se escribe en el banco de registros.</li></ol>\n<p>Completan la unidad de ejecución un banco de <strong>32 registros de propósito general</strong> de 32 bits, sumadores adicionales para calcular las direcciones de salto y una lógica de <em>adelantamiento de datos</em> (<em>forwarding</em>) que reduce las burbujas del pipeline. Junto a ella trabajan la unidad de multiplicación y división (MDU) y el coprocesador 0 (CP0), encargado de la traducción de memoria y del control de excepciones e interrupciones.</p>\n<div class=\"nota\"><strong>Consecuencia práctica del pipeline</strong> (la usaremos todo el curso): como varias instrucciones conviven dentro de la CPU a la vez, <em>protegemos las secciones críticas frente a las interrupciones</em> y <strong>no escribimos de manera consecutiva en un mismo registro</strong>: entre dos escrituras seguidas al mismo registro de un periférico se intercala otra instrucción (por ejemplo <code>asm(\"nop\");</code>).</div>\n</section>\n<section>\n<h2>5.3 Mapa de memoria: FMT, KSEG0 y KSEG1</h2>\n<p>El MIPS dispone de un bus de direcciones de 32 bits, por lo que el espacio de direcciones disponible es de 4&nbsp;GB. La arquitectura original, pensada para estaciones de trabajo, preveía memoria virtual con una MMU que tradujera páginas entre el espacio de cada proceso y la memoria física. En un microcontrolador <strong>solo hay un proceso</strong>, así que la relación entre memoria virtual y física se hace con una <strong>tabla fija</strong>: de ello se encarga la unidad <strong>FMT</strong> (<em>Fixed Mapping Translation</em>).</p>\n<p>Espacio de direcciones con un bus de 32 bits:</p><math class=\"formula\" xmlns=\"http://www.w3.org/1998/Math/MathML\" display=\"block\"><mrow><msup><mn>2</mn><mn>32</mn></msup><mtext>&#160;bytes</mtext><mo>=</mo><mn>4</mn><mtext>&#160;GB</mtext></mrow></math>\n<ul><li><strong>KSEG0</strong> es <em>cacheable</em>: su contenido puede almacenarse en la memoria caché de la CPU, en caso de que exista.</li><li><strong>KSEG1</strong> no es <em>cacheable</em>. Es el único que dispone del bloque <strong>SFRs</strong> (<em>Special Function Registers</em>), donde residen los registros de configuración de los periféricos: no tiene sentido cachearlos porque el hardware puede cambiarlos en cualquier momento y la copia en caché quedaría obsoleta.</li><li>Como el PIC32MX230F064D <strong>no tiene caché</strong>, se usa <strong>KSEG1 por defecto</strong>: todas las direcciones que manejaremos en el curso son de este segmento.</li></ul>\n<p>Los bloques de memoria definidos en el PIC32MX230F064D, con sus direcciones virtuales en KSEG1, son:</p>\n<ul><li><strong>16 kB de RAM</strong> a partir de <code>0xA0000000</code>: variables y pila. La pila crece hacia abajo desde el final del bloque y las variables globales se colocan en la parte baja.</li><li><strong>64 kB de FLASH de programa</strong> a partir de <code>0xBD000000</code>.</li><li><strong>1 MB reservado para los SFRs</strong> a partir de <code>0xBF800000</code>: registros de manejo de los periféricos (con muchos huecos, no hay tantos registros).</li><li><strong>3 kB de FLASH de arranque</strong> (<em>Boot Flash</em>) a partir de <code>0xBFC00000</code>.</li><li><strong>16 bytes (4 palabras de 32 bits)</strong> al final de la Boot Flash, en <code>0xBFC00BF0</code>, con los registros de configuración del micro (<code>DEVCFG3</code>–<code>DEVCFG0</code>).</li></ul>\n<p>Los SFR son posiciones de memoria como cualquier otra. En el examen ordinario de 2026 se prohibía usar el nombre <code>ADC1BUFn</code> y se exigía leer los resultados del ADC <strong>mediante punteros</strong>, sabiendo que <code>ADC1BUF0</code> está en <code>0xBF809070</code> (zona de SFRs de KSEG1) y que cada buffer está 0x10 bytes (16) más arriba. El <code>volatile</code> es obligatorio: el hardware cambia el registro sin que el compilador lo sepa.</p><pre><code class=\"lang-c\">#include &lt;stdint.h&gt;\n\n// ADC1BUF0 vive en la zona de SFRs de KSEG1.\n// Cada buffer ocupa 0x10 bytes (16) = 4 posiciones de 32 bits.\nvolatile uint32_t *adcbuf = (volatile uint32_t *)0xBF809070;\n\nuint32_t leeBuffer(uint8_t n) {\n    // Aritmetica de punteros: sumar 1 a un uint32_t* avanza 4 bytes,\n    // asi que para avanzar 0x10 bytes por buffer se suma 4*n\n    return *(adcbuf + 4*n);   // n=0 -&gt; 0xBF809070, n=1 -&gt; 0xBF809080...\n}</code></pre>\n<div class=\"nota\">Regla nemotécnica: todas las direcciones de SFR que verá en el curso empiezan por <code>0xBF8</code>. Los periféricos (timers, UART, ADC…) cuelgan de <code>0xBF80xxxx</code> y las interrupciones y los puertos A–C de <code>0xBF88xxxx</code>. Si una dirección empieza por <code>0xA000</code> es RAM y si empieza por <code>0xBD00</code> es FLASH de programa.</div>\n</section>\n<section>\n<h2>5.4 Acceso a memoria: alineación y little-endian</h2>\n<p>Desde el punto de vista del <strong>hardware</strong>, todos los accesos a memoria son de 32 bits = 4 bytes: cada vez que se lee o escribe algo, el bus mueve una palabra completa. Desde el punto de vista <strong>lógico</strong>, la unidad mínima de direccionamiento es el <strong>byte</strong>. Para que cada acceso lógico se resuelva con un único acceso físico, los datos deben estar <em>alineados</em>:</p>\n<ul><li>Las <strong>medias palabras</strong> (16 bits) solo pueden empezar en <strong>direcciones pares</strong>, para que quepan dentro de una misma palabra física.</li><li>Las <strong>palabras</strong> (32 bits) deben empezar en direcciones <strong>múltiplo de cuatro</strong>.</li><li>Un acceso no alineado obligaría a dos accesos a memoria y a recomponer el dato: la CPU directamente <strong>no lo permite</strong> (genera una excepción de error de dirección).</li></ul>\n<p>Por eso, en un vector de palabras de 32 bits (<code>uint32_t v[N]</code>) cada elemento ocupa 4 bytes y, si <code>v[0]</code> está alineado, todos los elementos quedan en direcciones múltiplo de 4:</p><math class=\"formula\" xmlns=\"http://www.w3.org/1998/Math/MathML\" display=\"block\"><mrow><mi>dir</mi><mo>&#8289;</mo><mo stretchy=\"false\">(</mo><mi>v</mi><mo stretchy=\"false\">[</mo><mi>i</mi><mo stretchy=\"false\">]</mo><mo stretchy=\"false\">)</mo><mo>=</mo><mi>dir</mi><mo>&#8289;</mo><mo stretchy=\"false\">(</mo><mi>v</mi><mo stretchy=\"false\">[</mo><mn>0</mn><mo stretchy=\"false\">]</mo><mo stretchy=\"false\">)</mo><mo>+</mo><mn>4</mn><mo>&#8901;</mo><mi>i</mi></mrow></math>\n<div class=\"nota\">El MIPS32 M4K del PIC32 ordena los bytes en <strong>little-endian</strong>: el byte <em>menos</em> significativo de la palabra se almacena en la dirección <em>más baja</em> de memoria. Así, dentro de la palabra de la dirección 0 están los bytes 0 (LSB), 1, 2 y 3 (MSB); en la palabra de la dirección 4, los bytes 4 a 7, etcétera.</div>\n<p>Esta aritmética de direcciones tiene una aplicación muy práctica con los registros de los periféricos. Los 16 buffers del ADC (<code class=\"reg\">ADC1BUF0</code>…<code class=\"reg\">ADC1BUFF</code>) son registros consecutivos del mapa de memoria… pero no contiguos: cada uno dista <strong>0x10 bytes</strong> del siguiente (entre medias viven sus registros CLR/SET/INV). En vez de leerlos uno a uno por su nombre, se pueden recorrer con un puntero:</p>\n<p>Recorrido de los 16 buffers del ADC con un puntero. La clave está en el índice: <code>buf</code> apunta a palabras de 32 bits, así que sumar 1 avanza 4 bytes; como de un buffer al siguiente hay 0x10 = 16 bytes (4 palabras), el buffer <em>i</em> está en <code>buf[4*i]</code>:</p><pre><code class=\"lang-c\">// Sumar las 16 medidas del ADC sin nombrar ADC1BUF0, ADC1BUF1, ...\nvolatile uint32_t *buf = &amp;ADC1BUF0;   // apunta al primer buffer\nuint32_t suma = 0;\nint i;\n\nfor (i = 0; i &lt; 16; i++) {\n    suma += buf[4 * i];   // buffer i = base + i*0x10 bytes = base + 4*i palabras\n}\n\n// Equivalente con el puntero \"andando\":\nbuf = &amp;ADC1BUF0;\nfor (i = 0; i &lt; 16; i++) {\n    suma += *buf;\n    buf += 4;             // +4 palabras = +0x10 bytes: siguiente buffer\n}</code></pre>\n<div class=\"nota\">Compruébalo en el mapa de memoria del datasheet: <code class=\"reg\">ADC1BUF0</code> está en 0xBF809070 y <code class=\"reg\">ADC1BUF1</code> en 0xBF809080 — 0x10 bytes de diferencia. Este truco reaparecerá en el Tema&nbsp;9 al recoger los resultados del escaneo multicanal del ADC.</div>\n</section>\n<section>\n<h2>5.5 Fases de desarrollo del firmware</h2>\n<p>El microcontrolador tiene pocos recursos, así que el programa no se desarrolla <em>en</em> él, sino en un PC con un <strong>compilador cruzado</strong>: un compilador que se ejecuta en un procesador (el de su ordenador) pero genera código para otra CPU distinta (el MIPS del PIC32). En el laboratorio usamos el compilador <strong>XC32</strong> dentro del entorno de desarrollo <strong>MPLAB X</strong>, y programamos en C, reservando el ensamblador para instrucciones muy concretas (como <code>asm(\"nop\")</code>).</p>\n<ol><li><strong>Editar</strong> (<em>Edit</em>): escribir los archivos <code>.c</code> y <code>.h</code> del proyecto en MPLAB&nbsp;X. <em>Sin tarjeta.</em></li><li><strong>Compilar y enlazar</strong> (<em>Build</em>): el XC32 traduce cada módulo a código máquina MIPS y el enlazador los une en un único ejecutable, asignando cada cosa a su bloque de memoria (código a FLASH, variables a RAM). <em>Sin tarjeta.</em></li><li><strong>Simular</strong> (<em>Simulator</em>): MPLAB&nbsp;X puede ejecutar el programa en el propio PC imitando la CPU del PIC32: puntos de parada, inspección de registros y variables, estímulos en los pines… Sirve para probar la lógica <em>antes</em> de tener el hardware (o sin él). <em>Sin tarjeta.</em></li><li><strong>Grabar</strong> (<em>Make and Program Device</em>): el programador transfiere el ejecutable a la FLASH del micro. <em>Con la tarjeta conectada</em> — pero solo durante la grabación: después el firmware vive en la FLASH y el micro arranca solo con alimentarlo, sin PC (<em>standalone</em>).</li><li><strong>Depurar</strong> (<em>Debug Run</em>): el M4K incluye hardware de depuración (ICD) que permite poner <em>breakpoints</em>, ejecutar paso a paso y vigilar variables (<em>watch</em>) mientras el programa corre en el chip real. <em>Con la tarjeta conectada al PC durante toda la sesión.</em></li></ol>\n<div class=\"nota\">Chuleta rápida: <strong>Build</strong> y <strong>Simulator</strong> no necesitan la tarjeta; <strong>Program</strong> la necesita un momento (luego el programa queda en la FLASH y corre solo); <strong>Debug</strong> la necesita conectada toda la sesión.</div>\n<div class=\"nota\">A diferencia de un programa de PC, el firmware de un microcontrolador <strong>no termina nunca</strong>: arranca al alimentar el circuito y solo 'muere' al apagarlo. Lo que en un PC sería un error (un bucle sin fin) aquí es la estructura normal del programa: el <em>bucle de scan</em>.</div>\n</section>\n<section>\n<h2>5.6 Arquitectura software: programación modular y top-down</h2>\n<p>La manera más elegante y fácil de construir un programa es dividir la tarea a realizar en otras tareas más simples (<strong>metodología top-down</strong>). Si estas tareas más simples no son aún lo suficientemente sencillas, se vuelven a dividir. El resultado es una <strong>programación modular</strong> con muchas ventajas:</p>\n<ul><li><strong>Reduce la complejidad</strong> del problema.</li><li>Permite usar transductores (sensores y actuadores) <strong>sin conocer su detalle</strong> interno.</li><li>Permite crear <strong>soluciones sencillas</strong> y facilita escribir y leer el código.</li><li><strong>Facilita el trabajo en equipo</strong>: cada miembro desarrolla un módulo.</li><li><strong>Aísla las causas de fallo</strong>: si falla el sensor, el error está en su módulo.</li><li>Los módulos son <strong>reutilizables</strong> en otros proyectos.</li><li>Hace que la <strong>compilación dure menos</strong>: solo se recompila lo que cambia.</li></ul>\n<p>En todo programa habrá un <strong>módulo principal</strong>, <code>main.c</code>, que concentra al resto de los módulos: es el único que puede ejecutar las funciones de los módulos auxiliares y el encargado de coordinar su ejecución. Y habrá uno o varios <strong>módulos auxiliares o drivers</strong>: archivos <code>.c</code> y <code>.h</code> separados que solo funcionan cuando son invocados por el módulo principal, y que resuelven cada una de las tareas concretas del problema. Cada módulo debe corresponder a una <strong>función lógica diferenciada</strong>:</p>\n<ul><li><strong>Lógicas de control</strong> (máquinas de estados, reguladores…).</li><li>Un <strong>sensor</strong> individual o un grupo de sensores similares.</li><li>Un <strong>actuador</strong> individual o un grupo de actuadores similares.</li><li><strong>Comunicaciones</strong> (UART, SPI, I2C…).</li><li>El tamaño de cada módulo es variable, pero deben ser <strong>pequeños</strong> para que sean claros y de poca complejidad. Si hay tareas recursivas (p. ej. controles), el módulo ofrece una función pensada para ser llamada desde el <em>bucle de scan</em>.</li></ul>\n<p>Esqueleto típico de <code>main.c</code> con la estructura que piden los exámenes: inicialización en el orden canónico y bucle de scan que coordina los drivers (aquí, los del examen de 2026: un sensor de llanto y un motor por PWM).</p><pre><code class=\"lang-c\">#include &lt;xc.h&gt;\n#include &lt;stdint.h&gt;\n#include \"sensor.h\"   // Drivers propios: entre comillas\n#include \"motor.h\"\n\nint main(void) {\n    LATB = 0; LATC = 0;               // 1. Salidas a 0 antes de fijar direccion\n    TRISB = 0xFFFF; TRISC = 0xFFFF;   // 2. Por defecto todo entradas...\n    ANSELB = 0; ANSELC = 0;           //    ...y digital\n\n    iniciaSensor(15);                 // 3. Cada modulo configura SUS pines\n    iniciaMotor(25);                  //    y SUS perifericos\n\n    while (1) {                       // 4. Bucle de scan: no termina nunca\n        uint8_t llanto = getLlanto(); //    Leer entradas (via getter)\n        if (llanto) {                 //    Algoritmo de control\n            mueveMotor(60);           //    Actualizar salidas\n        } else {\n            mueveMotor(0);\n        }\n    }\n    return 0;\n}</code></pre>\n</section>\n<section>\n<h2>5.7 Creación de drivers: el ejemplo valorPinB</h2>\n<p>Un driver consta de dos archivos, <code>driver.c</code> y <code>driver.h</code>. En el <strong>.h</strong> van todas las declaraciones de funciones que pueden ser usadas por un tercero —las funciones <em>públicas</em>— junto con una breve explicación para que se sepan usar, además de los <code>#include</code> necesarios para esas cabeceras. En el <strong>.c</strong> se definen todas las funciones y se declaran y definen las funciones <em>privadas</em> de uso interno, junto con los <code>#include</code> que necesite ese código.</p>\n<ul><li><strong>No puede haber variables externas</strong> (<code>extern</code>): las variables globales del módulo se declaran <code>static</code> en el <code>.c</code>, se establecen con <em>setters</em> y se leen con <em>getters</em>.</li><li>Las <strong>funciones privadas</strong> se declaran y definen solo en el <code>.c</code> (idealmente <code>static</code>): no aparecen en el <code>.h</code>.</li><li>Los <code>#include</code> de librerías de Microchip o del sistema van con ángulos, <code>#include &lt;xc.h&gt;</code>; los archivos nuestros van con comillas, <code>#include \"driver.h\"</code>.</li><li>Antes de programar un driver: <strong>ver cómo funciona el sensor/actuador y hacer el diseño hardware</strong> (a qué pin se conecta, niveles activos…).</li></ul>\n<p>Como ejemplo, se desea un driver <code>valorPinB</code> para leer y escribir de manera individual en los pines del puerto B. Tiene una variable global <code>pin</code> que indica el número de pin sobre el que actúan las demás funciones, y funciones para fijarlo (configurándolo como entrada, como salida, o sin configurarlo), leerlo y escribirlo.</p>\n<p><code>valorPinB.h</code>: solo las cabeceras públicas, comentadas para quien vaya a usar el driver, y los <code>#include</code> que esas cabeceras necesitan (<code>stdint.h</code> por los <code>uint8_t</code>).</p><pre><code class=\"lang-c\">// valorPinB.h - Driver para leer y escribir pines individuales del puerto B\n#include &lt;stdint.h&gt;\n\nuint8_t getPin(void);              // Devuelve el pin sobre el que actua el driver\nvoid PinAsInput(uint8_t numpin);   // Fija 'pin' y lo configura como entrada digital\nvoid PinAsOutput(uint8_t numpin);  // Fija 'pin' y lo configura como salida digital\nvoid setPin(uint8_t numpin);       // Fija 'pin' SIN configurarlo como entrada/salida\nuint8_t getPinValue(void);         // Lee el valor actual del pin\nvoid setPinValue(uint8_t v);       // Escribe v (0 o 1) en el pin</code></pre>\n<p><code>valorPinB.c</code>: la variable global del driver y la función auxiliar <code>initPin</code> son <code>static</code> (privadas, no aparecen en el <code>.h</code>). Fíjese en dos detalles heredados del pipeline: en <code>initPin</code> las dos escrituras a <code>TRISB</code> quedan separadas por la escritura a <code>ANSELB</code>, y en <code>setPinValue</code> se intercala un <code>nop</code> entre las dos escrituras consecutivas a <code>LATB</code>. Se escribe en <code>LATB</code> y se lee de <code>PORTB</code>.</p><pre><code class=\"lang-c\">#include &lt;xc.h&gt;          // &lt;&gt; para librerias de Microchip\n#include \"valorPinB.h\"   // \"\" para archivos nuestros\n\n// Funcion privada: se declara aqui, NO en el .h\nstatic void initPin(uint8_t numpin, uint8_t io);\n\nstatic uint8_t pin = 0;  // Variable global del driver (privada)\n\nuint8_t getPin(void) {\n    return pin;\n}\n\nstatic void initPin(uint8_t numpin, uint8_t io) {\n    pin = numpin;\n    TRISB &amp;= ~(1 &lt;&lt; numpin);      // De momento, salida...\n    ANSELB &amp;= ~(1 &lt;&lt; numpin);     // Pin digital (y separa las escrituras a TRISB)\n    TRISB |= (io &amp; 1) &lt;&lt; numpin;  // ...y entrada si io == 1\n}\n\nvoid PinAsInput(uint8_t numpin) {\n    initPin(numpin, 1);\n}\n\nvoid PinAsOutput(uint8_t numpin) {\n    initPin(numpin, 0);\n}\n\nvoid setPin(uint8_t numpin) {\n    pin = numpin;\n}\n\nuint8_t getPinValue(void) {\n    return (PORTB &gt;&gt; pin) &amp; 1;    // Se LEE de PORTB\n}\n\nvoid setPinValue(uint8_t v) {\n    LATB &amp;= ~(1 &lt;&lt; pin);          // Borra el bit (se ESCRIBE en LATB)\n    asm(\"nop\");                   // Evita dos escrituras seguidas al mismo registro\n    LATB |= (v &amp; 1) &lt;&lt; pin;       // Escribe el nuevo valor\n}</code></pre>\n<div class=\"nota\">Adelanto del tema de interrupciones: cuando una variable del módulo se comparta entre el <code>main</code> y una rutina de interrupción, además de declararse <code>volatile</code>, sus <em>getters</em> y <em>setters</em> deberán proteger el acceso con <code>asm(\"di\")</code> / <code>asm(\"ei\")</code>. Es el patrón que aparece en las soluciones de los exámenes.</div>\n</section>\n<section>\n<h2>Ejercicios de diseño de drivers</h2>\n<div class=\"mpi-mount\" data-componente=\"ejercicio-driver\" data-config='{\"ref\": \"arquitectura-mips-1\"}'></div>\n<div class=\"mpi-mount\" data-componente=\"ejercicio-driver\" data-config='{\"ref\": \"arquitectura-mips-2\"}'></div>\n</section>\n<section class=\"tema-cierre\"><h2>Resumen</h2><ul>\n<li>El PIC32MX230F064D integra una CPU <strong>MIPS32 M4K</strong> (núcleo licenciado por MIPS Technologies), memoria FLASH de programa, RAM para variables y pila, y los periféricos (puertos, timers, UART/SPI/I2C, ADC, USB).</li>\n<li>La CPU es <strong>load/store</strong> y está segmentada en un <strong>pipeline de 5 etapas</strong> (lectura, decodificación, ejecución, acceso a memoria, escritura). Consecuencia práctica: se protegen las secciones críticas frente a interrupciones y <strong>no se escribe dos veces seguidas en el mismo registro</strong> (se intercala un <code>nop</code>).</li>\n<li>El bus de direcciones de 32 bits da un espacio de 2<sup>32</sup> = 4&nbsp;GB. La unidad <strong>FMT</strong> traduce direcciones virtuales a físicas con una tabla fija. <strong>KSEG0</strong> es cacheable; <strong>KSEG1</strong> no lo es y contiene los <strong>SFRs</strong>. El PIC32MX230F064D, sin caché, usa KSEG1 por defecto.</li>\n<li>Bloques de memoria (KSEG1): <strong>16 kB de RAM</strong> en <code>0xA0000000</code>, <strong>64 kB de FLASH</strong> en <code>0xBD000000</code>, <strong>1 MB de SFRs</strong> en <code>0xBF800000</code>, <strong>3 kB de Boot Flash</strong> en <code>0xBFC00000</code> y 16 bytes de configuración (<code>DEVCFG3</code>–<code>DEVCFG0</code>).</li>\n<li>Los accesos físicos son de 32 bits pero se direcciona por bytes: las medias palabras deben empezar en direcciones <strong>pares</strong> y las palabras en <strong>múltiplos de 4</strong> (alineación). El orden de bytes es <strong>little-endian</strong>.</li>\n<li>El software se organiza de forma <strong>modular y top-down</strong>: un <code>main.c</code> que coordina (inicializaciones + bucle de scan) y <strong>drivers</strong> <code>.c</code>/<code>.h</code>, uno por función lógica (control, sensor, actuador, comunicaciones).</li>\n<li>En un driver: el <code>.h</code> lleva solo las funciones <strong>públicas</strong> comentadas; el <code>.c</code> las define junto a las funciones y variables <strong>privadas</strong> (<code>static</code>). Sin variables <code>extern</code>: las variables del módulo se acceden con <em>getters</em> y <em>setters</em>.</li>\n</ul></section>"
};
