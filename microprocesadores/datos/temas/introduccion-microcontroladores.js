/*
 * Contenido del tema "Introducción a los microcontroladores y al PIC32MX230F064D" (Tema 1).
 * GENERADO por herramientas/spec_a_tema.py desde datos/_specs/introduccion-microcontroladores.json — no editar a mano; edita el spec y regenera.
 */
window.MPI = window.MPI || {};
MPI.contenidoTemas = MPI.contenidoTemas || {};
MPI.ejercicios = MPI.ejercicios || {};
MPI.registros = MPI.registros || {};

MPI.ejercicios["introduccion-microcontroladores-1"] = {"titulo": "El programa principal de un sistema empotrado (Examen Ordinario 2021)", "enunciado": "<p>En el Examen Ordinario de 2021 se pedía el sistema de control de un <strong>espantapájaros electrónico</strong>: dos sensores de presencia detectan pájaros y dos servos mueven los brazos para ahuyentarlos. El <strong>programa principal</strong> ha de inicializar el sistema y, en el bucle de scan, leer el estado de los detectores de presencia y llamar a <code>actuarServos()</code> pasándole un 1 cuando alguno de los sensores detecte un pájaro cerca. Si <code>leerSensor()</code> devuelve un código de error se encenderá un LED para indicarlo (RC0 para el sensor derecho, RC1 para el izquierdo, ambos <strong>activos a nivel bajo</strong>). Por último, si <code>inicializarSensores()</code> devuelve un código de error, se encenderá un LED conectado a RC2 (activo a nivel bajo) y, como no tiene sentido continuar, el programa quedará bloqueado.</p>", "contexto": "<ul><li><code>int inicializarSensores(void)</code>: inicializa el I²C y los dos sensores; devuelve 0 si todo ha ido bien y −1 si ha habido algún fallo.</li><li><code>int leerSensor(int n_sensor)</code>: devuelve 1 si el sensor indicado (0 derecho, 1 izquierdo) detecta movimiento, 0 si no lo detecta y −1 si hay error en la medida.</li><li><code>void actuarServos(int hay_pajaro)</code>: mueve los brazos cuando recibe un 1 y los deja en reposo cuando recibe un 0.</li><li>LEDs de error: RC0 (sensor derecho), RC1 (sensor izquierdo) y RC2 (fallo de inicialización), todos activos a nivel bajo.</li></ul>", "preguntas": [{"texto": "¿Cuál es la estructura correcta del programa principal de este sistema?", "opciones": ["Un while(1) que en cada iteración inicializa los sensores, los lee y actúa sobre los servos.", "Inicializar el sistema una sola vez y después entrar en un while(1) que lee los sensores, decide y actúa sobre los servos.", "Leer los sensores, actuar sobre los servos y terminar el main() con return 0 para devolver el control al sistema operativo.", "Configurar una función que se ejecute automáticamente al detectar un pájaro, sin necesidad de bucle."], "correcta": 1, "explicacion": "<p>Es la estructura canónica del <strong>bucle de scan</strong>: la inicialización se hace <em>una sola vez</em> antes del bucle (configurar periféricos es lento y solo hay que hacerlo al arrancar) y el <code>while(1)</code> repite indefinidamente la secuencia <em>leer entradas → algoritmo → actualizar salidas</em>. Inicializar dentro del bucle sería un error grave, y en un microcontrolador no hay sistema operativo al que «devolver el control»: el <code>main()</code> no debe terminar nunca.</p>"}, {"texto": "inicializarSensores() devuelve −1 al arrancar. ¿Qué debe hacer el programa según el enunciado?", "opciones": ["Encender el LED de RC2 escribiendo un 1 en ese pin y continuar con el bucle de scan.", "Llamar a exit(-1) para terminar el programa con código de error.", "Encender el LED de RC2 escribiendo un 0 en ese pin y bloquearse en un bucle infinito vacío.", "Reiniciar el microcontrolador automáticamente hasta que la inicialización tenga éxito."], "correcta": 2, "explicacion": "<p>Sin sensores el sistema no puede funcionar, así que se avisa del error y se bloquea el programa con <code>while(1);</code>. Como el LED es <strong>activo a nivel bajo</strong>, para encenderlo hay que poner el pin RC2 a 0 (por ejemplo con <code>LATCCLR = 1 &lt;&lt; 2;</code>). <code>exit()</code> no tiene sentido en un microcontrolador: no hay sistema operativo que recoja la salida del programa.</p>"}, {"texto": "Durante el funcionamiento normal, ¿qué debe ocurrir en cada iteración del bucle de scan?", "opciones": ["Llamar a actuarServos() solo cuando ambos sensores detecten un pájaro a la vez.", "Leer los sensores únicamente cuando los servos estén parados, para no perder tiempo.", "Esperar a que un sensor detecte un pájaro y entonces salir del bucle para mover los servos.", "Leer ambos sensores, encender el LED de error correspondiente si alguno devuelve −1 y llamar a actuarServos() con un 1 si alguno detecta un pájaro."], "correcta": 3, "explicacion": "<p>El enunciado pide pasar un 1 a <code>actuarServos()</code> «cuando <strong>alguno</strong> de los sensores detecte un pájaro» (OR lógico, no AND) y gestionar el error de cada sensor con su LED (RC0 derecho, RC1 izquierdo). En el bucle de scan <em>todas</em> las tareas se ejecutan en cada iteración: leer entradas, decidir y actualizar salidas; nunca se sale del bucle.</p>"}, {"texto": "En un PC, un while(1) del que no se sale se considera un programa «colgado». ¿Por qué aquí es la solución correcta?", "opciones": ["Porque el microcontrolador ejecuta un único programa que debe controlar el sistema sin parar mientras esté alimentado; si main() terminara, el chip dejaría de hacer nada útil.", "Porque el compilador XC32 no permite que la función main() termine.", "Porque el bucle infinito mantiene la memoria FLASH activa y evita que se borre el programa.", "Porque así el microcontrolador consume menos energía que si el programa terminara."], "correcta": 0, "explicacion": "<p>En un microcontrolador <strong>solo hay un programa</strong>, que arranca al alimentar el chip y debe ejecutarse hasta que se apague: no hay sistema operativo ni «siguiente programa» al que ceder el control. La FLASH es no volátil y no necesita refresco, y el consumo no depende de que el programa termine. El bucle sin fin es simplemente la estructura natural del software empotrado.</p>"}]};
MPI.ejercicios["introduccion-microcontroladores-2"] = {"titulo": "Conoce tu microcontrolador: el PIC32MX230F064D", "enunciado": "<p>Antes de escribir una sola línea de código conviene conocer el hardware con el que trabajarás todo el curso. Responde a las siguientes preguntas sobre el <strong>PIC32MX230F064D</strong> del laboratorio, usando los datos de su <em>datasheet</em> y la configuración de relojes de la placa.</p>", "contexto": "<ul><li>Microcontrolador: PIC32MX230F064D, encapsulado de 44 pines, núcleo MIPS32 M4K.</li><li>Configuración de relojes del laboratorio: SYSCLK = 40&nbsp;MHz y divisor del bus de periféricos PBDIV = 8.</li><li>Herramientas: compilador XC32 y entorno MPLAB&nbsp;X sobre un PC.</li></ul>", "preguntas": [{"texto": "¿En qué memoria se almacena el programa para que no se pierda al quitar la alimentación, y de cuánta dispone este chip?", "opciones": ["En la memoria RAM, de 16 kB.", "En la memoria FLASH, de 64 kB (más 3 kB de boot flash).", "En una EEPROM externa conectada por I²C.", "En los registros internos de la CPU MIPS32."], "correcta": 1, "explicacion": "<p>La memoria de programa ha de ser <strong>no volátil</strong> y hoy en día es casi siempre <strong>FLASH</strong> por su facilidad de borrado y reprogramación (sustituyó a las ROM de fábrica y a las EPROM borrables por ultravioleta). El PIC32MX230F064D tiene <strong>64&nbsp;kB de FLASH</strong> de programa más 3&nbsp;kB de <em>boot flash</em>. La RAM de 16&nbsp;kB es volátil: solo guarda los datos mientras hay alimentación.</p>"}, {"texto": "Con la configuración del laboratorio, ¿a qué frecuencia funciona el bus de periféricos (PBCLK)?", "opciones": ["40 MHz, la misma que el procesador.", "8 MHz, el valor del divisor PBDIV.", "5 MHz, resultado de dividir los 40 MHz de SYSCLK entre PBDIV = 8.", "13,1 MHz, la frecuencia máxima del temporizador."], "correcta": 2, "explicacion": "<p>f<sub>PBCLK</sub> = f<sub>SYSCLK</sub> / PBDIV = 40&nbsp;MHz / 8 = <strong>5&nbsp;MHz</strong>. El bus de periféricos funciona más lento que la CPU para ahorrar energía. Este dato es importantísimo: PBCLK es el reloj que alimenta a temporizadores, UART y ADC, y lo usarás en todos los cálculos de periodos del curso.</p>"}, {"texto": "Quieres medir la tensión analógica que entrega un sensor de temperatura. ¿Qué periférico del PIC32MX230F064D debes usar?", "opciones": ["El conversor analógico-digital, con 13 canales de entrada y 10 bits de resolución.", "Un comparador analógico, que devuelve directamente el valor de la tensión.", "La CTMU, diseñada para medir tensiones analógicas.", "El Parallel Master Port (PMP), que digitaliza la señal en paralelo."], "correcta": 0, "explicacion": "<p>El <strong>ADC</strong> convierte una tensión analógica en un número digital: el de este chip tiene <strong>13 canales</strong>, <strong>10 bits</strong> de resolución y hasta 1&nbsp;Msps. El comparador analógico solo indica si la señal supera un umbral (no mide su valor), la CTMU mide capacidades (sensores táctiles) y el PMP es un interfaz paralelo de 8 bits para comunicarse con otros dispositivos.</p>"}, {"texto": "Para programar el PIC32 usamos en el PC el compilador XC32, que es un «compilador cruzado». ¿Qué significa eso?", "opciones": ["Que puede compilar a la vez programas escritos en C y en ensamblador.", "Que se ejecuta en una CPU (la del PC) pero genera código máquina para otra distinta (el MIPS32 del PIC32).", "Que se ejecuta dentro del propio microcontrolador, cruzando el código por el bus de periféricos.", "Que traduce el código C a Java para que sea portable entre microcontroladores."], "correcta": 1, "explicacion": "<p>El microcontrolador tiene demasiado pocos recursos para alojar un compilador, así que se desarrolla en un ordenador con un <strong>compilador cruzado</strong>: se ejecuta en el microprocesador del PC pero genera código para otra CPU, el MIPS32 del PIC32. El ejecutable resultante se graba después en la FLASH del chip desde MPLAB&nbsp;X.</p>"}]};

MPI.contenidoTemas["introduccion-microcontroladores"] = {
  titulo: "Introducción a los microcontroladores y al PIC32MX230F064D",
  html: "<header class=\"tema-cab\">\n<span class=\"tema-num\">Tema 1</span>\n<h1>Introducción a los microcontroladores y al PIC32MX230F064D</h1>\n<p class=\"tema-lead\">Un microcontrolador es un <strong>ordenador completo en un solo chip</strong>: CPU, memoria de programa, memoria de datos y periféricos. En este tema conocerás el <strong>PIC32MX230F064D</strong> que usaremos todo el curso y la estructura de programa universal de los sistemas empotrados: el <em>bucle de scan</em>.</p>\n</header>\n<section>\n<h2>1.1 ¿Qué es un microcontrolador?</h2>\n<p>En Sistemas Digitales I vimos que hay dos formas de implantar un sistema digital: mediante <strong>lógica cableada</strong>, en la que la «inteligencia» del sistema viene dada por la forma de conectar las puertas lógicas, y mediante <strong>lógica programada</strong>, en la que la «inteligencia» viene dada por el programa que ejecuta un microcontrolador. Esta asignatura estudia la segunda forma, que es generalmente la más flexible y económica.</p>\n<p>Desde su aparición allá por 1974 el uso de los microcontroladores no ha parado de crecer: hoy el <strong>98&nbsp;% de los procesadores fabricados son microcontroladores</strong>; el 2&nbsp;% restante son los microprocesadores de ordenadores, <em>smartphones</em> y servidores. El secreto de su éxito radica en dos factores:</p>\n<ul><li><strong>Bajo precio:</strong> con un microcontrolador que cuesta menos de un euro se implantan circuitos que con lógica discreta necesitarían varios chips, ocuparían más espacio y consumirían más energía.</li><li><strong>Flexibilidad:</strong> como la funcionalidad se define mediante <em>software</em>, el circuito se adapta a nuevos requisitos sin tocar el hardware: basta cambiar el programa.</li></ul>\n<div class=\"nota\">Para hacerse una idea de hasta dónde llega la miniaturización: el PIC10F320T es un microcontrolador de 8 bits con 448 <em>bytes</em> de memoria de programa (sí, bytes), 64 bytes de RAM y solo 8 patillas… y cuesta 0,36&nbsp;€.</div>\n</section>\n<section>\n<h2>1.2 Están en todas partes (y tú vas a aprender a mandar sobre ellos)</h2>\n<p>Haz la cuenta de hoy: el despertador, la cafetera, el microondas, el mando del garaje, el ascensor, el patinete, los auriculares… y un coche moderno lleva <strong>más de 70</strong>. Detrás de cada uno hay un microcontrolador ejecutando un programa que alguien escribió. Esta asignatura va exactamente de eso: de que ese alguien seas tú.</p>\n<ul><li><strong>En una lavadora:</strong> lee los botones y el sensor de nivel de agua, controla el motor del tambor y la resistencia, y cronometra cada fase del lavado.</li><li><strong>En un dron:</strong> lee el giróscopo y el acelerómetro cientos de veces por segundo y reajusta la velocidad de las cuatro hélices para que no se caiga.</li><li><strong>En un coche:</strong> decenas de microcontroladores se reparten el ABS, los airbags, el climatizador o los elevalunas, y hablan entre ellos por un bus de comunicaciones.</li><li><strong>En una estación meteorológica:</strong> mide temperatura y humedad (señales analógicas), las convierte en números y los envía por radio o USB a un ordenador.</li></ul>\n<p>¿Y qué sabrás hacer tú al acabar el curso? Las mismas piezas con las que se construyen todos esos productos:</p>\n<ul><li>Encender, apagar y leer cualquier cosa digital: LEDs, pulsadores, relés… (Tema 3).</li><li>Medir y generar tiempos con precisión de microsegundos (Tema 4).</li><li>Reaccionar a los eventos en el instante en que ocurren, sin esperas (Tema 6: interrupciones).</li><li>Hablar con un ordenador o con otros chips (Temas 7 y 8: UART, I²C y SPI).</li><li>Medir el mundo real: tensiones, temperaturas, luz… (Tema 9: el conversor A/D).</li><li>Regular potencia y mover motores y servos (Tema 10: PWM).</li><li>Y juntarlo todo en un producto que funciona (Temas 11 y 12).</li></ul>\n<div class=\"nota\">De hecho, el curso termina construyendo un producto completo: en el Tema&nbsp;12 diseñaremos el <strong>termostato de una clase</strong>, con sus sensores de temperatura, sus interruptores, su ventilador y su display. Todo lo que aprendas por el camino son las piezas de ese puzle.</div>\n</section>\n<section>\n<h2>1.3 Tu herramienta de trabajo: el PIC32MX230F064D</h2>\n<p>Todo el curso usaremos el mismo chip: el <strong>PIC32MX230F064D</strong> de Microchip. Por unos 2&nbsp;€ tienes un ordenador completo en 44 patillas: una CPU de 32 bits (MIPS32) a 40&nbsp;MHz, <strong>64&nbsp;kB de memoria FLASH</strong> donde vive el programa (no volátil: no se borra al quitar la alimentación), <strong>16&nbsp;kB de RAM</strong> para los datos y una colección de periféricos para tocar el mundo exterior. ¿Te parece poca memoria comparada con los gigas de tu portátil? Los programas de un microcontrolador son pequeños y van al grano: sobra.</p>\n<p>Dentro del chip conviven dos relojes: el de la CPU (SYSCLK) y el del bus de periféricos (PBCLK), que va más despacio para ahorrar energía. Con la configuración del laboratorio (SYSCLK = 40&nbsp;MHz, PBDIV = 8):</p><math class=\"formula\" xmlns=\"http://www.w3.org/1998/Math/MathML\" display=\"block\"><mrow><msub><mi>f</mi><mtext>PBCLK</mtext></msub><mo>=</mo><mfrac><msub><mi>f</mi><mtext>SYSCLK</mtext></msub><mtext>PBDIV</mtext></mfrac><mo>=</mo><mfrac><mrow><mn>40</mn><mtext>&#8201;MHz</mtext></mrow><mn>8</mn></mfrac><mo>=</mo><mn>5</mn><mtext>&#8201;MHz</mtext></mrow></math>\n<div class=\"nota\">Memoriza estos dos números: en el laboratorio <strong>SYSCLK = 40&nbsp;MHz</strong> y <strong>PBCLK = 5&nbsp;MHz</strong>. El valor de PBCLK aparecerá una y otra vez durante el curso, porque es el reloj que alimenta a los temporizadores, las UART, el ADC…</div>\n<p>Cómo es por dentro (el <em>pipeline</em> de la CPU, los buses, el mapa de memoria) lo veremos con calma en el Tema&nbsp;5. Por ahora, quédate con la foto general: CPU + FLASH + RAM + periféricos, todo en un chip.</p>\n</section>\n<section>\n<h2>1.4 Los periféricos del PIC32MX230F064D</h2>\n<p>Los periféricos son los que permiten al microcontrolador interactuar con el mundo exterior, y son el motivo por el que cada familia de microcontroladores es distinta. Según su <em>datasheet</em>, el PIC32MX230F064D (encapsulado de 44 pines, 33 de E/S) incluye:</p>\n<ul><li><strong>Puertos de entrada/salida digital:</strong> tres puertos de 16 bits (A, B y C), aunque no todos los bits están disponibles en los pines. Como entrada permiten leer el nivel de tensión de un pin (un pulsador); como salida permiten fijarlo (un LED).</li><li><strong>Temporizadores/contadores:</strong> 5 temporizadores de 16 bits, que permiten contar eventos externos o medir tiempos, con unidades de captura (<em>Input Capture</em>) para medir frecuencias o anchos de pulso y de comparación (<em>Output Compare</em>) para generar señales PWM.</li><li><strong>Interfaces de comunicación:</strong> 2 UART (comunicación asíncrona) y 2 SPI/I²S y 2 I²C (síncrona), para conectarse a otros ordenadores o a dispositivos como memorias o pantallas LCD.</li><li><strong>Conversor analógico-digital:</strong> 13 canales de entrada analógica con resolución de 10 bits y hasta 1&nbsp;Msps, para medir señales analógicas.</li><li><strong>Comparadores analógicos:</strong> 3 comparadores que detectan cuándo una señal supera un umbral seleccionable entre 32 niveles de tensión.</li><li><strong>Controlador USB 2.0 OTG:</strong> permite conectarse a un ordenador o, gracias a <em>USB On-The-Go</em>, a dispositivos como memorias USB.</li><li><strong>CTMU</strong> (<em>Charge Time Measurement Unit</em>): mide capacidades, útil para sensores táctiles capacitivos.</li><li><strong>PMP</strong> (<em>Parallel Master Port</em>): interfaz paralelo de 8 bits para comunicarse con pantallas LCD, memorias u otros microcontroladores.</li><li><strong>DMA:</strong> 4 canales programables y 2 dedicados, para mover datos sin intervención de la CPU.</li></ul>\n<div class=\"nota\">De toda esta lista, los periféricos que estudiaremos en detalle durante el curso son los <strong>puertos digitales</strong> (Tema 3), los <strong>temporizadores</strong> (Tema 4), la <strong>UART</strong>, el <strong>I²C/SPI</strong>, el <strong>ADC</strong> y el <strong>PWM con Output Compare</strong>. El resto conviene saber que existen.</div>\n</section>\n<section>\n<h2>1.5 El software: compilador cruzado y entorno de desarrollo</h2>\n<p>Un microcontrolador recién salido de fábrica no es más que un trozo de silicio inerte. Para darle «vida» hay que escribir un programa que le indique cómo interactuar con su entorno. Como el microcontrolador tiene pocos recursos, el programa se desarrolla en un ordenador usando un <strong>compilador cruzado</strong>: un compilador que se ejecuta en un microprocesador (por ejemplo un i7) pero genera código para otro tipo de CPU, en nuestro caso el MIPS32 del PIC32.</p>\n<p>En el laboratorio usaremos el compilador <strong>XC32</strong> junto con el entorno de desarrollo <strong>MPLAB&nbsp;X</strong>, que facilita la gestión del proyecto, la grabación del programa en la FLASH y la depuración. La mayoría de los microcontroladores se programan en <strong>C</strong>; solo excepcionalmente se recurre al ensamblador para optimizar un trozo de código o acceder a algún recurso específico de la CPU.</p>\n<p>Hay una diferencia fundamental con los programas de un ordenador de sobremesa: aquellos se cargan en memoria, se ejecutan y al terminar (llamando a <code>exit()</code> o llegando al final del <code>main()</code>) dejan paso al siguiente programa. En un microcontrolador <strong>solo hay un programa</strong>, que arranca al alimentar el chip y <strong>no debe terminar nunca</strong> hasta que se apague. Por eso, mientras que en un PC un bucle sin fin es un error que deja el programa «colgado», en un microcontrolador es justo la estructura normal del programa.</p>\n</section>\n<section>\n<h2>1.6 El bucle de scan</h2>\n<p>En la mayoría de los casos la finalidad de un microcontrolador es <strong>controlar algún dispositivo</strong>. La estructura de programa más sencilla para ello es un bucle sin fin denominado <strong>bucle de scan</strong>: primero se inicializa el sistema (configurando los periféricos para adaptarse al sistema a controlar) y después se entra en un bucle infinito que repite sin descanso la secuencia <em>leer entradas → ejecutar el algoritmo de control → actualizar salidas</em>.</p>\n<p>Esqueleto de un programa para el PIC32MX230F064D con la estructura de bucle de scan. Fíjate en el orden de la inicialización: primero las salidas a un valor seguro con <code>LAT</code>, después la dirección de los pines con <code>TRIS</code> y por último los periféricos (los detalles de estos registros se ven en el Tema&nbsp;3):</p><pre><code class=\"lang-c\">#include &lt;xc.h&gt;\n#include &lt;stdint.h&gt;\n\nint main(void)\n{\n    /* === 1. Inicializacion del sistema === */\n    LATA = 0;            // Salidas a 0 antes de configurar su direccion\n    LATB = 0;\n    LATC = 0;\n    TRISA = 0xFFFF;      // Por defecto todos los pines como entrada;\n    TRISB = 0xFFFF;      // cada driver configurara despues los suyos\n    TRISC = 0xFFFF;\n\n    // Aqui se llama a las funciones de inicializacion de cada\n    // periferico: inicializarSensores(), inicializarServos()...\n\n    /* === 2. Bucle de scan: no termina nunca === */\n    while (1) {\n        // Leer las entradas\n        // Ejecutar el algoritmo de control\n        // Actualizar las salidas\n    }\n\n    return 0;            // Nunca se llega aqui\n}</code></pre>\n<p>Esta secuencia <em>lectura–ejecución–actualización</em> se repite una y otra vez para controlar el sistema en el que está integrado el microcontrolador, y solo termina cuando se apaga el dispositivo. En los exámenes de la asignatura el programa principal sigue <strong>siempre</strong> esta estructura, con un detalle adicional: si la inicialización de un periférico falla y no tiene sentido continuar, el programa avisa del error (por ejemplo encendiendo un LED) y se bloquea en un bucle infinito vacío.</p>\n<p>Patrón de manejo de errores de inicialización usado en los exámenes (aquí con un LED de error en RC2, <strong>activo a nivel bajo</strong>: se enciende escribiendo un 0):</p><pre><code class=\"lang-c\">if (inicializarSensores() != 0) {   // Fallo en la inicializacion\n    LATCCLR = 1 &lt;&lt; 2;               // Enciende el LED de error (RC2,\n                                    // activo a nivel bajo)\n    while (1) {\n        ;                           // Bloquea el programa: sin sensores\n    }                               // no tiene sentido continuar\n}</code></pre>\n<div class=\"nota\"><strong>Ampliación opcional:</strong> el bucle de scan es suficiente para sistemas sencillos, pero tiene un problema: si una tarea tarda mucho, retrasa a todas las demás. En el Tema&nbsp;6 veremos cómo las <em>interrupciones</em> permiten atender eventos urgentes sin esperar a que el bucle dé la vuelta.</div>\n</section>\n<section>\n<h2>Ejercicios de diseño de drivers</h2>\n<div class=\"mpi-mount\" data-componente=\"ejercicio-driver\" data-config='{\"ref\": \"introduccion-microcontroladores-1\"}'></div>\n<div class=\"mpi-mount\" data-componente=\"ejercicio-driver\" data-config='{\"ref\": \"introduccion-microcontroladores-2\"}'></div>\n</section>\n<section class=\"tema-cierre\"><h2>Resumen</h2><ul>\n<li>Un <strong>microcontrolador</strong> integra en un solo chip CPU, memoria de programa, memoria de datos y periféricos; el 98&nbsp;% de los procesadores fabricados son microcontroladores gracias a su bajo precio y a que su funcionalidad se define por <em>software</em>.</li>\n<li>El <strong>PIC32MX230F064D</strong> tiene un núcleo MIPS32 M4K con <em>pipeline</em> de 5 etapas, conectado por los buses IS (instrucciones) y DS (datos) a través de una <em>Bus Matrix</em> que permite accesos simultáneos.</li>\n<li>Dos relojes a recordar: <strong>SYSCLK = 40&nbsp;MHz</strong> (CPU y bus rápido) y <strong>PBCLK = SYSCLK/8 = 5&nbsp;MHz</strong> (bus de periféricos).</li>\n<li>Memorias: <strong>64&nbsp;kB de FLASH</strong> (no volátil, para el programa) y <strong>16&nbsp;kB de RAM</strong> (volátil, para los datos).</li>\n<li>Periféricos principales: 3 puertos digitales de 16 bits, 5 temporizadores, 2 UART, 2 SPI, 2 I²C, ADC de 10 bits y 13 canales, USB OTG, 3 comparadores, CTMU y PMP.</li>\n<li>Se programa en <strong>C</strong> con el <strong>compilador cruzado XC32</strong> y el entorno <strong>MPLAB&nbsp;X</strong>: el compilador corre en el PC pero genera código MIPS32.</li>\n<li>La estructura universal del software empotrado es el <strong>bucle de scan</strong>: inicializar el sistema una vez y repetir sin fin <em>leer entradas → ejecutar algoritmo → actualizar salidas</em>; si una inicialización falla, se avisa (LED de error) y se bloquea el programa en un bucle vacío.</li>\n</ul></section>"
};
